# Audit: chất lượng & tường minh (2026-07-26)

Phương pháp: 15 agent (7 auditor theo chiều — identity-scoping, silent-failures, visibility-surfaces, config-dead-state, external-deps, error-quality, session-integrity — mỗi finding bị 1 verifier đối kháng cố bác bỏ, + 1 completeness critic). **81 finding sống sót / 1 bị bác**. Mọi finding có file:line + snippet đã đọc thật.

Mức độ: P0 = mất dữ liệu/context hoặc chủ động đánh lừa user · P1 = hành vi đã hứa âm thầm degrade/no-op · P2 = state tồn tại nhưng không surface nào hiện · P3 = vệ sinh.

## P0 — 9 finding

### WS-1 [identity-scoping] Project identity silently forks: slug key flips between remote-URL and \\?\-canonical path depending on whether git spawns

- **Vị trí:** `src/core/config.rs:162` · verdict CONFIRMED
- **Hệ quả:** Every launch where git resolvability differs (PowerShell without git vs Git Bash/VS Code, or a remote added later) silently re-keys the ENTIRE per-project state: memory zone (store scope), frozen core file, learned skills zone (skills/mod.rs:94), and /init codebase index (codebase.rs:215) all fork. Facts and skills the user saved yesterday are unreachable today with zero notice, no migration, and no error — effectively data loss the user cannot even diagnose.
- **Bằng chứng:** let stable_key = git_remote_origin(&root).unwrap_or_else(|| { std::fs::canonicalize(&root).unwrap_or_else(|_| root.clone()).display().to_string() });  — Confirmed in the user's data: C:/Users/admin/.aizen/cli-memory/core/active/ holds BOTH aizen-web-8279a5b7.md and aizen-web-6e885184.md for the same checkout. I recomputed fnv1a64-as-u32: 8279a5b7 = hash of "\\?\C:\Users\admin\Desktop\mini_project\aizen_web" (path key, git not spawnable) and 6e885184 = hash of its remote.origin.url (URL key, git spawnable). Same twin pair exists for aizen-be (750ac895 / d4a1274e, created 15 min apart on 26/07), plus admin-e4df4c78 = hash of "\\?\C:\Users\admin" and system32-fbb7562a = hash of "\\?\C:\Windows\System32".
- **Hướng sửa:** Make the slug key derivation deterministic and single-sourced: always hash a normalized canonical root path (strip the \\?\ verbatim prefix, lowercase drive letter — reuse workspace_txn::normalized_path), and treat remote.origin.url as an ALIAS recorded in a small sidecar map (slug -> {root, remote}) so a re-clone can be re-linked explicitly. On startup, if a zone keyed by the other derivation exists for the same dirname, either auto-merge or print one line: "memory zone forked: <a> vs <b> — run `aizen memory migrate`". Never let PATH luck choose the key.
- **Verifier:** Quote exact at src/core/config.rs:162-167. Independently recomputed every hash claim: 8279a5b7=fnv32('\\?\C:\Users\admin\Desktop\mini_project\aizen_web'), 6e885184=fnv32(aizen_web remote.origin.url read from .git/config), d4a1274e=fnv32('\\?\...\aizen_be' path), e4df4c78=fnv32('\\?\C:\Users\admin'), fbb7562a=fnv32('\\?\C:\Windows\System32'); bonus: aizen-621459ef.md = URL-hash of this repo's remote. Both aizen-web twins and both aizen-be twins (07:07/07:22 on 26/07) exist in ~/.aizen/cli-memory/core/active. Dependents verified: skills/mod.rs:94, codebase.rs:215, frozen_core.rs:38-41, store.rs scope. P0 stands: all four per-project stores silently re-key with no surface, no migration.

### WS-2 [identity-scoping] Sessions are project-blind and /resume grafts the OTHER project's stable system lane byte-for-byte onto the current cwd

- **Vị trí:** `src/main.rs:6529` · verdict CONFIRMED
- **Hệ quả:** Launch aizen in project A, press /resume on the offered "last conversation" that actually belongs to project B: the restored stable lane still carries B's <project_context> and B's frozen core while every tool executes against A's cwd. The model is actively misled about which codebase it is in, and the session picker gives the user no project column to notice — the classic outcome is edits applied to the wrong tree with full confidence.
- **Bằng chứng:** fn save_session(...) { ... let mut bytes = serde_json::to_vec_pretty(history)?; ... }  — only the raw message vector is persisted, no project root/slug field; sessions_dir() (main.rs:6464) is a single flat ~/.aizen/sessions for all projects; load_session (main.rs:6543) calls refresh_dynamic_prompt_lane whose contract is "refresh only the dynamic lane, preserving stable index 0 byte-for-byte" (main.rs:4918); the startup hint (main.rs:3754) offers "⟲ last conversation … /resume to continue it" from ANY project. Observed: ~/.aizen/sessions/ mixes chats from aizen, aizen_web, aizen-be, HOME ("hãy-vào-thư-mục-aizenbe-0726.json", "vào-thư-mục-aizenweb-rồi-0723.json") in one pool with one shared last.json.
- **Hướng sửa:** Record {project_slug, project_root} in each session file (a wrapper object, with legacy bare-array fallback). On load, if the stored root differs from the current root: rebuild the stable lane for the CURRENT project (refresh_prompt_lanes_in_place already exists) and print "session was saved in <root B> — context lane rebuilt for <root A>". Show the project name in the /sessions picker and scope the startup /resume hint to sessions whose root matches, falling back to a clearly-labeled cross-project offer.
- **Verifier:** All cited lines exact (main.rs:6529 to_vec_pretty(history) only; 6464 global flat dir; 6543 refresh_dynamic_prompt_lane; 4918 'preserving stable index 0 byte-for-byte'; 3754 cross-project /resume hint via last.json pointer at 6575-6576). Observed ~/.aizen/sessions mixes aizen/aizen_web/aizen-be/HOME chats incl. both quoted filenames, one shared last.json. sessions_menu (main.rs:6773-6781) renders only name + msg count — no project column is even possible since the file carries no project field. P0 stands.

### SW-01 [silent-failures] Every-turn conversation autosave swallows all write errors — silent total loss of the promised safety net

- **Vị trí:** `src/main.rs:6752` · verdict CONFIRMED
- **Hệ quả:** autosave_last() is the ONLY writer behind per-turn autosave AND the exit flush (flush_live_session_on_exit → autosave_last, main.rs:6671-6674, also invoked from the Windows console-close handler at 6707). save_session returns Result with full context (main.rs:6524-6536) but both calls discard it, and no emit_line/eprintln exists anywhere on this chain. If ~/.aizen/sessions becomes unwritable (disk full, Controlled Folder Access, permissions), every turn silently saves nothing, the exit flush silently saves nothing, and the user discovers only after quitting that the whole conversation is gone — while explicit /sessions save at main.rs:6821 DOES surface errors, so the user has been trained to trust saves.
- **Bằng chứng:** let _ = save_session(history, &name);
        if name != "last" {
            let _ = save_session(history, "last"); // keep `last` as the "most recent" pointer
        }
... let _ = crate::core::recovery::checkpoint_history(  (main.rs:6752-6757). The doc two functions down promises: "The live chat is also auto-saved as `last` after every turn, so there's always something to come back to." (main.rs:6766-6767)
- **Hướng sửa:** Propagate or at least surface the first failure: change autosave_last to match on save_session's Result and emit one throttled warning line via tui::emit_line (e.g. 'warning: autosave failed: {e:#} — your conversation is NOT being saved; try /sessions save'), latching a static AtomicBool so it prints once per distinct failure streak and clears on the next success. The exit-flush path should eprintln! on failure since the TUI may already be torn down.
- **Verifier:** Evidence exact at main.rs:6752-6757; doc promise at 6766-6767 verbatim. autosave_last is the sole writer for ALL autosave paths (per-turn 4079/4089/4158/4380, autosave_session:6604, exit flush 6673, Windows console handler 6707) and swallows every Result with no emit/eprintln anywhere on the chain. save_session (6524-6536) returns Result with full context. Explicit /sessions save DOES surface errors at 6821-6829, confirming the trained-trust contrast. P0 fits: a persistent write failure silently defeats the sole safety net and the whole conversation is lost on exit with zero warning.

### VIS-01 [visibility-surfaces] Active memory/skill zone is invisible; 'remembered' confirmations omit it, so observed zone drift silently orphans saved facts

- **Vị trí:** `src/main.rs:4732` · verdict CONFIRMED
- **Hệ quả:** The user is told a fact was saved, but with git off PATH the slug hashes canonicalize(cwd) and drifts (two zones per project observed on disk). The next session reads a different zone, so the confirmed fact never comes back — the user was actively told 'remembered' about data that is effectively lost, and has zero surface to detect it (memory entries print 'zone <slug>' per entry in meta_line, but nothing ever shows which zone is CURRENT).
- **Bằng chứng:** "{}remembered {n_durable} fact{} — /memory to view" (and #-shortcut at src/main.rs:5345: "🧠 remembered ({id})"). The zone is computed in src/core/config.rs:162-167 from git_remote_origin(...).unwrap_or_else(|| canonicalize(root)...) and is rendered NOWHERE in the TUI. Observed in C:/Users/admin/.aizen/cli-memory/core/active/: TWO drift pairs — aizen-be-750ac895.md + aizen-be-d4a1274e.md AND aizen-web-6e885184.md + aizen-web-8279a5b7.md — plus system32-fbb7562a.md.
- **Hướng sửa:** Append the active zone to every save confirmation: "🧠 remembered ({id}) → zone aizen-be-750ac895" in preprocess_input and maybe_learn_memory, and print a startup banner line "project: <root> · zone <slug>" in splash::render_inner (it already prints model/endpoint/key at lines 405-423). Add the zone to /memory profile output. This makes drift immediately visible the day it happens instead of surfacing as amnesia weeks later.
- **Verifier:** All evidence reproduced: main.rs:4732 'remembered {n_durable} fact… — /memory to view' and main.rs:5345 '🧠 remembered ({id})' omit the zone; memory::remember (src/memory/mod.rs:498) scopes the fact to config::project_slug(), computed at config.rs:162-167 from git_remote_origin().unwrap_or_else(canonicalize(root)). Grep shows zero project_slug()/project_root() call sites in any UI renderer; splash (src/ui/splash.rs:405-423) prints only version/model/endpoint/key; zone appears only per-entry via zone_tag (mod.rs:605) and meta_line 'zone {z}' (mod.rs:709), never the CURRENT zone. On-disk drift verified: ~/.aizen/cli-memory/core/active/ holds aizen-be-750ac895.md and aizen-be-d4a1274e.md written 15 minutes apart on 26/07/2026, plus aizen-web-6e885184.md + aizen-web-8279a5b7.md and system32-fbb7562a.md. Frozen core and retrieval key off the same slug (frozen_core.rs:38), so a drifted zone's facts never return in normal Current-scope use. P0 (told 'remembered', data effectively unreachable next session, no surface to detect it) stands.

### EXT-1 [external-deps] git off PATH hard-blocks EVERY agent file edit (auto-checkpoint fails closed with a misdirected error)

- **Vị trí:** `src/agent/mod.rs:1967` · verdict CONFIRMED
- **Hệ quả:** On this exact machine (git.exe exists but is not on PATH in PowerShell), every Paths/OpaqueWorkspace tool call — file_write, multi_edit, file_move, everything — is refused on every attempt, in EVERY directory (repo or not, because RepoContext::discover runs `git rev-parse` before it can decide repo-ness). The agent's core capability is dead, the per-tool error blames "the pre-edit checkpoint", and nothing at startup says "git is not installed/on PATH". Incoherent with existing posture: a plain non-repo folder already gets edits with zero checkpoint protection via the benign path.
- **Bằng chứng:** Err(e) => Some(format!("error: protected workspace change was not run because the pre-edit checkpoint failed: {e:#}")) — reached because timemachine.rs:404 maps a git-binary ENOENT to `return Err(e).context("time machine could not use git in this directory")`, which does NOT contain "not a git repository", so save_protected_change (timemachine.rs:1350) propagates Err instead of the benign Ok(None); auto_checkpoint defaults true (agent/mod.rs:513 `auto_checkpoint: true,`)
- **Hướng sửa:** Detect git-binary absence as a distinct, cached state (see EXT-3 resolver). When git is absent, take the same benign path as "not a git repository" (checkpoints off, edits allowed) but emit ONE loud line: "⚠ git not found on PATH — Time Machine/auto-checkpoint OFF for this session; install git or set AIZEN_GIT". Keep fail-closed only for real git errors (dubious ownership, corrupt store) where git exists but the snapshot genuinely failed.
- **Verifier:** Full chain reproduced: timemachine.rs:1065-1069 raw_git ENOENT -> context 'running git (is git installed?)' which does NOT contain 'not a git repository', so discover (timemachine.rs:399-405) returns Err with 'time machine could not use git in this directory'; save_protected_change:1348-1352 substring check misses -> Err propagates; agent/mod.rs:1944-1969 refuses the tool ('...pre-edit checkpoint failed'); auto_checkpoint defaults true (mod.rs:513) and needs_checkpoint covers Paths|OpaqueWorkspace (tools.rs:26-28) = file_write/file_edit/multi_edit/file_move/shell_run. Applies in EVERY directory since git ENOENT can never produce the benign substring. No startup/one-time git-missing warning exists anywhere in src (grep). One nuance: the {e:#} chain does end with 'running git (is git installed?): <os error>', so the root cause is buried-but-present in each per-tool error; the P0 substance (total edit outage, fail-closed asymmetry vs. the benign checkpoint-less non-repo path, zero proactive surface) is exact.

### EXT-2 [external-deps] Project identity silently forks depending on git availability — same repo gets two memory/skill zones

- **Vị trí:** `src/core/config.rs:162` · verdict CONFIRMED
- **Hệ quả:** The slug hash keys on remote.origin.url when git resolves, and on the canonicalized path string when it doesn't (which also flips between `\\?\C:\…` and plain `C:\…` when canonicalize itself fails). Launch aizen from Git Bash (git on PATH) vs PowerShell (git off PATH) and the SAME repo lands in two different memory zones — durable memory, skills, and per-project state silently split, and no surface ever shows which identity/zone is active or why.
- **Bằng chứng:** let stable_key = git_remote_origin(&root).unwrap_or_else(|| { std::fs::canonicalize(&root).unwrap_or_else(|_| root.clone()).display().to_string() }); — git_remote_origin (line 178) returns None on ANY git failure including binary-not-found. Observed under C:/Users/admin/.aizen/cli-memory/core/active/: aizen-be-750ac895.md AND aizen-be-d4a1274e.md (verified present) for the same project.
- **Hướng sửa:** Route slug computation through the central git resolver so the key source is deterministic per machine; strip the `\\?\` verbatim prefix before hashing so canonicalize success/failure can't change the hash; print the active zone slug + its key source ("origin-url" vs "path") in the startup banner and /status; on fallback from url→path (or vice versa) detect the sibling zone file and offer a merge.
- **Verifier:** config.rs:161-167 quoted exactly; git_remote_origin (config.rs:177-192) returns None on .output().ok()? i.e. any failure incl. binary-not-found; fallback canonicalize success gives \\?\-prefixed string vs root.clone() plain string on failure -> three possible hash keys for one directory. Observed data re-verified: C:/Users/admin/.aizen/cli-memory/core/active/ contains aizen-be-750ac895.md AND aizen-be-d4a1274e.md, both last written 26/07/2026. Slug keys frozen-core memory (frozen_core.rs:38-41), skills zone (skills/mod.rs:94), codebase index (codebase.rs:215). No surface shows the active identity or key source: project_slug/project_root have zero display call sites; status_text (main.rs:3613-3643) shows model/tokens/persona only; memory inventory (memory/mod.rs:661, zone_tag :605-610) tags entries [p:zone] but never states which zone is active or why. P0 stands: durable project memory/skills silently fork on launch-shell PATH differences.

### SESS-01 [session-integrity] /handoff keeps the old session slug live, so the next autosave overwrites the previous conversation's saved transcript with the handoff stub

- **Vị trí:** `src/main.rs:6093` · verdict CONFIRMED
- **Hệ quả:** /handoff returns SlashOutcome::Submit, so a turn runs immediately; at its end autosave_last saves the tiny post-handoff history under the OLD conversation's file name (and last.json), clobbering the full prior transcript (observed session files are 300-400 KB). The recovery sidecar is also re-checkpointed (main.rs:6757), so the pre-handoff conversation is irrecoverably destroyed with zero warning — the UI says "fresh thread seeded", never "your old thread's save file will be replaced".
- **Bằng chứng:** "handoff" arm (6093-6096): `rebuild_system(history, model_label); history.push(Message::system(format!("[handoff context from the previous session]\n{summary}")));` — no `set_session_slug(None)`, unlike the /clear arm at 5975: `set_session_slug(None); // the next turn names + autosaves a brand-new session file`. autosave_last (6742-6754) then writes under `current_session_slug()`.
- **Hướng sửa:** In the handoff success branch, mirror /clear: call set_session_slug(None) (and update_live_history) after rebuild_system, so the post-handoff thread allocates its own file on first autosave and the old named file is left intact. Optionally emit "previous conversation kept as '<slug>'" so the user knows where it went.
- **Verifier:** Reproduced exactly. /handoff arm (src/main.rs:6082-6108) calls rebuild_system + pushes the '[handoff context...]' seed but never set_session_slug(None) or update_live_history, unlike /clear (5975-5976). rebuild_system (4985-4988) and handoff_now (5593-5609) do not touch the slug. Handoff returns SlashOutcome::Submit (6100), so at turn end autosave_last (6742-6762) writes the tiny post-handoff history under the OLD slug via current_session_slug(), overwriting the named file AND last.json AND the recovery sidecar (6757) AND the exit-flush snapshot (6756). Time-machine checkpoints cover repo files, not ~/.aizen/sessions, so the prior transcript is irrecoverable. P0 severity correct.

### SESS-02 [session-integrity] One global flat sessions dir + shared last.json across all projects/instances: startup hint and bare /resume restore whichever project's conversation wrote last, with no project label or validation

- **Vị trí:** `src/main.rs:6576` · verdict CONFIRMED
- **Hệ quả:** Two concurrent aizen instances in two projects both rewrite last.json after every turn; last writer wins. Opening aizen in project A then shows "⟲ last conversation \"<project B's chat>\" — /resume to continue it" (main.rs:3746-3759) and bare /resume (5997-6001) restores project B's entire history into project A with no project check, no confirmation, and no label saying which directory it came from. Two instances resumed into the same named session silently interleave-clobber each turn (atomic_write prevents torn files but there is no lock, no lost-update detection). This is the mechanism behind the user's "lost/mixed context" complaint.
- **Bằng chứng:** `let last_bytes = std::fs::read(sessions_dir().join("last.json")).ok()?;` (6576) with sessions_dir() = `config::nextgen_home().join("sessions")` (6465) and the unconditional pointer write `let _ = save_session(history, "last"); // keep `last` as the "most recent" pointer` (6754). Observed: C:/Users/admin/.aizen/sessions holds sessions from at least 4 different projects in one flat dir (hãy-vào-thư-mục-aizenbe-0726.json, vào-thư-mục-aizenweb-rồi-0723.json, cusersadmindesktopcauut-…-0725.json, httpsgithubcomdiegosouzapwomniroute-…-0726.json).
- **Hướng sửa:** Partition sessions per project (subdir keyed by project slug, with the slug drift bug fixed) or store a provenance header (project root, model, timestamps) in each session file; make most_recent_session prefer the current project's newest session and label the hint with the source directory; detect concurrent writers (mtime/generation check before autosave) and warn instead of silently clobbering.
- **Verifier:** All evidence reproduced: sessions_dir() = nextgen_home()/sessions (6464-6466), most_recent_session reads global last.json (6576), unconditional pointer write (6753-6754), startup hint (3746-3759) and bare /resume (5997-6009) restore with zero project label/validation, save_session (6524-6536) uses atomic_write with no lock or generation check. Observed dir listing today: 13 files from at least 5 distinct projects (hãy-vào-thư-mục-aizenbe-0726, vào-thư-mục-aizenweb-rồi-0723, cusersadmindesktopcauut-...-0725, httpsgithubcomdiegosouzapwomniroute-...-0726, kết-nối-claude-với-telegram) sharing one last.json (byte-identical to the aizenbe file). Session files carry no provenance metadata (save_session serializes only the message vec). P0 correct.

### SESS-03 [session-integrity] Restore never validates or refreshes the stable system lane: a restored session silently keeps the foreign <environment> cwd, model name, and <project_context> baked in at save time

- **Vị trí:** `src/main.rs:6543` · verdict CONFIRMED
- **Hệ quả:** Resume a session (via /resume, /sessions, or the cross-project last.json path of SESS-02) while sitting in a different directory or on a different model, and the agent is permanently told the WRONG cwd, wrong date, wrong model identity, and the other project's AGENTS.md/CLAUDE.md conventions — for every remaining turn (the per-turn path at 3873-3874 also only refreshes the dynamic lane). The agent will resolve relative paths and follow house rules for the wrong project; nothing on screen indicates the mismatch.
- **Bằng chứng:** load_session (6537-6546) does only `*history = loaded; migrate_legacy_prompt_lanes(history, model); refresh_dynamic_prompt_lane(history, model);` and refresh_dynamic_prompt_lane is documented at 4918 as "refresh only the dynamic lane, preserving stable index 0 byte-for-byte". The stable lane contains the environment and project context (src/agent/mod.rs:310-313 `bundle.stable.push_str("\n<project_context>\n")`). Observed in ~/.aizen/sessions/hello-0726.json message[0]: `<environment>\ncwd: C:\Users\admin\nos: windows\ndate: 2026-07-26\nmodel: grok-4.5\n</environment>`.
- **Hướng sửa:** On load_session, compare the restored stable lane's environment (cwd/model) against the live one; if they differ, rebuild the stable lane via refresh_prompt_lanes_in_place (accepting the one-time prefix-cache bust) and print an explicit notice: "session was saved in <dir> on <model>; environment updated to <current>". Store provenance metadata with the session so this check is cheap and exact.
- **Verifier:** load_session (6537-6546) does only migrate_legacy_prompt_lanes (no-op when lead>=2, 4903-4907) + refresh_dynamic_prompt_lane; the 4918 doc comment 'preserving stable index 0 byte-for-byte' is verbatim. Environment block and <project_context> are built into the STABLE lane (src/agent/mod.rs:306-313). Per-turn path (3873-3874) also refreshes only the dynamic lane, so the stale lane persists all session. Observed ~/.aizen/sessions/hello-0726.json message[0]: '<environment> cwd: C:\Users\admin / os: windows / date: 2026-07-26 / model: grok-4.5'. Worse than claimed: the /resume success line (6010-6017) prints 'context restored', actively affirming a wrong environment. P0 correct.

## P1 — 34 finding

### WS-3 [identity-scoping] git-not-on-PATH is misdiagnosed as "not a git repository (run `git init`)" and turns the benign no-repo degrade into a hard block of every edit tool

- **Vị trí:** `src/features/timemachine.rs:1350` · verdict CONFIRMED
- **Hệ quả:** A user sitting inside a perfectly good git repo (git.exe installed, just not on PATH) is told the repo doesn't exist and to run `git init` — wrong diagnosis AND wrong remediation — while all file-editing tools fail, typically pushing the model to bypass protections via raw shell writes. The designed degrade ("checkpoints simply off, edits proceed") is unreachable in this state.
- **Bằng chứng:** Err(e) if e.to_string().contains("not a git repository") => Ok(None),  — the ONLY benign escape in save_protected_change. When git cannot spawn, raw_git (timemachine.rs:1069) errors "running git (is git installed?)" which does NOT match, so agent/mod.rs:1967 returns "error: protected workspace change was not run because the pre-edit checkpoint failed" for EVERY workspace-mutating tool (auto_checkpoint defaults true, agent/mod.rs:513). Meanwhile is_repo() (timemachine.rs:1324, RepoContext::current().is_ok()) collapses the spawn failure to false, so checkpoint tools reply "error: not a git repository — checkpoints need git (run `git init`)" (timemachine.rs:2191). Verified live: this user's PowerShell reports "git : The term 'git' is not recognized".
- **Hướng sửa:** Distinguish the three states in RepoContext::discover: (a) git spawned, not a repo -> benign Ok(None)/is_repo=false; (b) git failed to SPAWN -> a distinct GitUnavailable error carrying "git was not found on PATH (found at C:\Program Files\Git\cmd\git.exe? add it or set AIZEN_GIT)" surfaced once per session; (c) real git errors -> propagate. Resolve the git binary once (PATH, then common install locations, then an AIZEN_GIT override) and reuse it for every Command::new("git") in config.rs/timemachine.rs/workspace_txn.rs.
- **Verifier:** All quotes exact (timemachine.rs:1350, 1069, 1324, 2191; agent/mod.rs:513 auto_checkpoint:true, 1967-1969). Verified discover() (timemachine.rs:399-405) wraps spawn failure as 'time machine could not use git in this directory' which fails the 'not a git repository' filter, so the benign Ok(None) degrade is unreachable and every checkpoint-gated tool errors. One nuance: the edit-tool error formats {e:#} so its chain does include 'running git (is git installed?)' — but the checkpoint_save tool's is_repo() path (2190-2191) still misdiagnoses 'not a git repository — run git init' inside a real repo, and the designed degrade never fires. P1 right.

### WS-4 [identity-scoping] project_root() silently falls back to cwd-at-launch, inverting the documented R4 promise and minting fake projects (HOME, System32, any subdir)

- **Vị trí:** `src/core/config.rs:73` · verdict CONFIRMED
- **Hệ quả:** With git off PATH every launch directory IS the project: skills auto-learned while working on aizen-be are filed under the C:\Users\admin zone and silently vanish (skills::list() reads only the current zone, skills/mod.rs:186-196) when the user later launches inside the real project; memory facts, frozen core and /init index scatter the same way. The R4 subdir guarantee silently no-ops with no warning that "project = C:\Users\admin" was chosen.
- **Bằng chứng:** std::env::current_dir().unwrap_or_else(|_| PathBuf::from("."))  — the fallback after the git shell-out; the doc four lines up promises "Repo-root-aware so launching `ng` from a SUBDIR still finds the repo's `.nextgen/` (R4 — fixes the cwd-relative footgun)" (config.rs:55-56). Observed consequences: ~/.aizen/skills/p/ contains exactly ONE zone, admin-e4df4c78 (hash-verified = \\?\C:\Users\admin) holding 14 auto-learned skill files that belong to real projects, and a system32-fbb7562a.md core file exists (elevated-shell default cwd); session names ("hãy vào thư mục aizen-be…") show the user launches from HOME and asks the agent to cd — subshell cd never moves the identity.
- **Hướng sửa:** When the git probe fails to spawn (see WS-3 fix) fall back to an upward FILESYSTEM search for a .git directory from cwd (pure Rust, no git needed) before surrendering to cwd. Additionally, when the resolved root is the user's home dir or a system dir, print a one-line warning at startup ("treating C:\Users\admin as the project — launch from your repo or set NG_PROJECT_ROOT") since that is almost never intended.
- **Verifier:** config.rs:73 quote exact; R4 promise verbatim at lines 54-56. No pure-Rust upward .git search fallback exists anywhere; no home/system-dir warning. Data reproduced: skills/p/ has exactly one zone admin-e4df4c78 with 14 files (finding's count of 14 is the accurate on-disk count today; the seed's '17' is stale), names clearly from aizen_be/aizen_web work (add-axum-api-contract-tests, add-bilingual-site-switcher, sync-install-pages-with-release-artifacts...). skills::list() (mod.rs:186-196) reads only global + current zone + repo dir. Session file 'hãy-vào-thư-mục-aizenbe-0726.json' confirms the launch-from-HOME pattern.

### WS-5 [identity-scoping] 115 recovery lease dirs (21.4 MB of full transcripts) accumulate invisibly: the window-close handler never clears the lease and nothing GCs or lists them

- **Vị trí:** `src/main.rs:6707` · verdict CONFIRMED
- **Hệ quả:** Every ✕-closed session leaves a permanent lease containing the entire transcript (potentially pasted secrets) that no surface ever shows and no command can delete unless the user happens to relaunch from the identical scope — and when the scope DOES match, the user is greeted with a "⟳ recoverable session" banner for a chat that was actually flushed cleanly, which misrepresents what happened. Growth is unbounded.
- **Bằng chứng:** let _ = std::panic::catch_unwind(flush_live_session_on_exit);  — the CTRL_CLOSE/LOGOFF/SHUTDOWN handler flushes the chat but never calls recovery::clear() (which runs only on the graceful path, main.rs:4183). Observed: C:/Users/admin/.aizen/recovery/ holds 115 lease dirs dated 21/07–26/07, 21.4 MB total; sampled 1785046633-21564 contains history.61.json (383 KB full conversation) + manifest.json. scan_stale offers only same-repo_scope leases (recovery.rs:285) and /recover discard (main.rs:6030-6034) can also only touch same-scope ones.
- **Hướng sửa:** Call recovery::clear() (or write a tombstone marker) from the exit-flush handler after the session flush succeeds. Add age-based GC in scan_stale for leases older than N days regardless of scope, and give /recover a `list --all` view showing every lease with its repo_scope and age so the user can see and purge cross-scope leftovers.
- **Verifier:** main.rs:6707 quote exact; handler (6704-6710) only calls flush_live_session_on_exit, and recovery::clear() is called at exactly one site — main.rs:4183, the graceful path. Data reproduced: 115 lease dirs, 21.4 MB under ~/.aizen/recovery; sampled 1785046633-21564 holds history.61.json (383,894 bytes) + manifest.json + lease.lock. scan_stale filters on repo_scope at recovery.rs:285 and /recover discard (main.rs:6030-6034) iterates only those offers; grep confirms no age-based GC (remove_dir_all only in clear/consume/quarantine-rename/tests). The misleading '⟳ recoverable session' banner after a clean window-close flush also holds (main.rs:3743-3745 suppresses the honest 'last conversation' hint in its favor).

### WS-6 [identity-scoping] Crash recovery is keyed to the launch-dir identity, so a crashed session's history+draft are only ever offered when relaunching from the exact same directory

- **Vị trí:** `src/core/recovery.rs:84` · verdict CONFIRMED
- **Hệ quả:** The user's natural reaction to a crash — "reopen aizen in the project folder" — guarantees the recovery offer (safe history + the unsent draft they typed) never appears; it sits silently among the 115 leases forever. The user concludes the work is gone. The same mismatch fires between a git-on-PATH shell (scope = repo toplevel) and a git-less shell (scope = cwd) for the same repo.
- **Bằng chứng:** pub fn current_repo_scope() -> String { let root = crate::core::config::project_root(); fs::canonicalize(&root).unwrap_or(root).display().to_string() }  — combined with scan_stale's exact-string filter `if manifest.repo_scope != repo_scope { continue; }` (recovery.rs:285). With git off PATH project_root() = cwd (config.rs:73), so a session launched from C:\Users\admin that crashed while editing aizen-be is scoped to "\\?\C:\Users\admin".
- **Hướng sửa:** Store both the canonical root and a normalized repo identity (workspace_txn::WorkspaceIdentity.repo_id, which already degrades sanely) in the manifest and match on either; when leases exist for OTHER scopes, print one dim line at startup ("1 recoverable session from another folder — /recover list") instead of total silence.
- **Verifier:** recovery.rs:83-86 quote exact (current_repo_scope = canonicalized project_root display string); exact-string filter at 285 exact; project_root() cwd fallback at config.rs:73 verified. begin() (recovery.rs:117-146) stamps the scope at launch, so a crash while editing another dir via subshell cd is scoped to the launch dir forever. No cross-scope listing surface exists. P1 right.

### WS-7 [identity-scoping] Workspace writer lock identity degrades to a different key without git, so two aizen processes from different shells take different 'exclusive' locks on the same repo

- **Vị trí:** `src/core/workspace_txn.rs:46` · verdict CONFIRMED
- **Hệ quả:** The WorkspaceWriterLease's exclusive-writer guarantee (acquired per edit tool at agent/mod.rs:1915) silently evaporates exactly in the mixed-shell scenario this machine lives in: two concurrent sessions can both hold an "exclusive" workspace lock and interleave edits/checkpoints with no error, no warning, and no way to notice until the tree is corrupted.
- **Bằng chứng:** let repo_key = canonical_common_git_dir.as_ref().or(canonical_git_dir.as_ref()).unwrap_or(&canonical_root); let repo_id = stable_key(&format!("repo:{}", normalized_path(repo_key)));  — git_path() (workspace_txn.rs:310) returns None whenever git cannot spawn, so a PowerShell-launched aizen keys locks on SHA256(root) while a Git-Bash-launched aizen keys the same repo on SHA256(common git dir). ~/.aizen/locks/v1/repo/ already shows 6 distinct repo ids.
- **Hướng sửa:** Derive repo_id from a git-independent anchor when git is unavailable: walk up from the canonical root to find a .git dir in pure Rust and normalize THAT path with the existing normalized_path(), so both environments hash the same key. Log (trace-level) which identity basis was used so a disagreement is at least diagnosable.
- **Verifier:** workspace_txn.rs:46-50 quote exact; git_path (line 309-310) returns None on spawn failure via .output().ok()?, so repo_key degrades from canonical common git dir to canonical root — two different SHA256 lock ids for the same repo across git-on-PATH vs git-less shells. Observed 6 distinct repo-id dirs under ~/.aizen/locks/v1/repo (64-hex names). Lease acquired per edit tool at agent/mod.rs:1915 on cwd. No trace/log of which identity basis was used. P1 right.

### SW-02 [silent-failures] /yolo and /smart print 'approval → {mode}' even when the config save failed — approval mode actually unchanged

- **Vị trí:** `src/main.rs:6216` · verdict CONFIRMED
- **Hệ quả:** Because approval_mode() re-loads cli-config.json on every check, a failed save means the toggle did literally nothing at runtime — yet the UI unconditionally announces the new mode. Worst direction: user in yolo runs /yolo to turn auto-approval OFF, sees 'approval → ask', and aizen keeps executing commands without prompting. The sibling command /approval at main.rs:6204-6207 matches on the save Result and prints the error, so this is an accidental swallow in the legacy aliases, not policy.
- **Bằng chứng:** cfg.set_approval_mode(mode);
            let _ = cli_config::save(&cfg);
            tui::emit_line(&style(format!("approval → {mode} (legacy /yolo alias)"))...  (main.rs:6214-6217; same pattern for /smart at 6220-6224). Runtime reads the mode back from DISK: pub fn approval_mode() -> ApprovalMode { ... load().persisted_approval_mode() } (src/core/cli_config.rs:455-460).
- **Hướng sửa:** Make /yolo and /smart use the same match cli_config::save(&cfg) { Ok(_) => success line, Err(e) => red error line } shape as the /approval handler at main.rs:6204-6207, and only print the mode-changed message on Ok.
- **Verifier:** let _ = cli_config::save at main.rs:6216 (/yolo) and 6223 (/smart) with unconditional 'approval → {mode}' prints. Critical premise verified: cli_config::load() (cli_config.rs:613-634) re-reads disk every call with no in-memory cache, and approval_mode() (455-461) routes through it — so a failed save means the runtime mode is genuinely unchanged while the UI announces the flip. Sibling /approval at 6204-6207 matches on the Result, proving accidental swallow. Worst direction (yolo stays on after user sees 'approval → ask') is real.

### SW-03 [silent-failures] MCP trust prompt swallows trust_project()/dismiss_project() write failure, then prints '✓ trusted — its tools are now available.'

- **Vị trí:** `src/main.rs:3520` · verdict CONFIRMED
- **Hệ quả:** This is the explicit security gate for repo-shipped MCP servers. If the trust-file write fails, the user is told the servers are trusted and loaded, but project_trust_prompt() (mcp.rs:228-237) will still consider the repo untrusted — the servers' tools stay unavailable this and every launch, and the scary prompt re-nags, contradicting both printed messages ('won't nag again'). The user's answer to a security question silently evaporates with a false success confirmation.
- **Bằng chứng:** if ok {
        let _ = crate::agent::mcp::trust_project();
        println!("{}", style("✓ trusted — its tools are now available.")...
    } else {
        let _ = crate::agent::mcp::dismiss_project();
        println!(... "skipped — run `aizen mcp trust` anytime to enable." ...) (main.rs:3519-3525). trust_project() persists via update_trust(...) -> Result (src/agent/mcp.rs:197-205) and dismiss via mcp.rs:217-224.
- **Hướng sửa:** Match on trust_project()/dismiss_project() Results: print the success line only on Ok; on Err print the error with the trust-file path so the user can fix permissions, and skip the 'tools are now available' claim.
- **Verifier:** main.rs:3519-3525 exact: let _ = trust_project()/dismiss_project() followed by unconditional success/skip prints. trust_project/dismiss_project return Result via update_trust→save_trust (mcp.rs:197-224). Consequence verified: project_trusted() re-reads the trust file and gates project server loading at mcp.rs:124 (and verify_gate.rs:97/118), and project_trust_prompt() (228-238) will re-nag — so '✓ trusted — its tools are now available.' is false in-session AND on every later launch when the write fails.

### SW-04 [silent-failures] update_bots() parses a corrupt bots.json as empty and then REWRITES it — silently destroys all hosted-bot registrations

- **Vị trí:** `src/hostbot/store.rs:79` · verdict CONFIRMED
- **Hệ quả:** One truncated/corrupt bots.json (partial write, hand edit) makes the next user-initiated /addbot or /rmbot read the list as empty and then atomically write back only the mutation result — permanently erasing every previously added sub-bot, tokens and persona bindings included, with zero warning. Contrast: cli_config::load() explicitly warns once on corruption and save() keeps a .json.bak (src/core/cli_config.rs:621-631, 659-662); bots.json gets neither the warning nor the backup.
- **Bằng chứng:** let mut bots = match std::fs::read_to_string(&path) {
        Ok(s) => serde_json::from_str(&s).unwrap_or_default(),
        Err(_) => Vec::new(),
    };
    let result = mutate(&mut bots)?;
    ... atomic_write_owner_only(&path, ...)  (store.rs:78-84); load_bots() has the same silent unwrap_or_default at store.rs:66.
- **Hướng sửa:** In update_bots, distinguish 'file missing' (fresh install, empty vec is right) from 'file exists but does not parse': on parse failure, bail with the serde error and keep the file untouched (or copy it to bots.json.bak and warn, mirroring save_unlocked in cli_config.rs), so a user mutation can never silently clobber the existing roster.
- **Verifier:** store.rs:78-81 exact: corrupt bots.json parses as empty via unwrap_or_default, then mutate + atomic_write_owner_only at 84 rewrites the file — /addbot (add_bot_entry, telegram.rs:698) or /rmbot (telegram.rs:714) permanently erases the prior roster including tokens and persona bindings. load_bots has the same swallow at store.rs:66. Contrast verified: cli_config warns once on corruption (cli_config.rs:624-630) and preserves .json.bak before clobbering (659-662); bots.json gets neither. P1 right.

### SW-05 [silent-failures] In-REPL auto-learn drops the whole learn report on error and never shows rejected/review outcomes — an explicit 'remember X' can silently no-op

- **Vị trí:** `src/main.rs:4725` · verdict CONFIRMED
- **Hệ quả:** Natural-language 'remember: …' is routed as UserExplicit durable memory through exactly this path (learning/mod.rs:133-134). If the store write fails (ingest Err), or the fact is threat-scan rejected, or it lands in the human review queue (Route::Review writes to review_dir at learning/mod.rs:198), the chat shows nothing at all — no 'remembered' line, no rejection reason, no pointer to `aizen memory review`. The user believes the fact was saved; it wasn't, and the only surfaces that would tell them are CLI-only commands they were never pointed to.
- **Bằng chứng:** let report = match learning::ingest(&user_text, &opts) {
        Ok(r) => r,
        Err(_) => return, // best-effort; never disrupt the REPL
    }; (main.rs:4723-4726). The success path then surfaces ONLY added+reinforced and session_notes (main.rs:4727-4750); report.rejected, report.queued_review and report.dropped are never rendered in the REPL, while the CLI twin prints them ('✗ rejected {fact} — {why}', '? review {id} (run `aizen memory review`)', src/memory/mod.rs:918-931).
- **Hướng sửa:** In maybe_learn_memory: (1) on Err, emit one dim warning line ('memory learn failed: {e:#}') instead of returning silently; (2) extend the success rendering to cover report.queued_review ('N fact(s) queued for review — aizen memory review') and report.rejected (dim one-liner with the reason), mirroring print_learn_report in src/memory/mod.rs so the REPL and CLI surfaces agree.
- **Verifier:** main.rs:4723-4726 exact (Err(_) => return); success path renders only added+reinforced (4729-4739) and session_notes (4740-4750). Grep confirms 'queued_review' and '.rejected' appear nowhere in main.rs — the REPL never surfaces them — while the CLI twin prints '? review … (run `aizen memory review`)' and '✗ rejected …' at memory/mod.rs:918-926. Routing verified: Remember/Correction → UserExplicit at learning/mod.rs:133-134; Route::Review writes review_dir at 198; threat-scan rejection at 161-163. An explicit 'remember X' can indeed silently vanish into rejection, the review queue, or an ingest error.

### SW-06 [silent-failures] Telegram pairing owner capture swallows the config save — 'paired' owner silently lost on restart

- **Vị trí:** `src/hostbot/platforms/telegram.rs:538` · verdict CONFIRMED
- **Hệ quả:** The pairing-code flow is the sole owner-capture mechanism for a fresh `aizen serve --token` deployment. If cli_config::save fails (locked file, permissions), pairing appears to succeed in-session — the bot answers the owner — but after the daemon restarts the allowlist is empty again: the owner is locked out of their own bot and must re-pair, with no log line ever explaining why. The docstring's promise ('survives a restart') silently degrades.
- **Bằng chứng:** /// Persist a freshly-paired owner chat id into `cli-config.json` so it survives a restart.
fn persist_owner(chat: i64) {
    ...
    cfg.telegram = Some(tg);
    let _ = cli_config::save(&cfg);
} (telegram.rs:530-539); a second swallowed roster write exists at telegram.rs:714 (let _ = store::update_bots(...)).
- **Hướng sửa:** Propagate the save Result out of persist_owner and, on Err, send the owner chat a message ('warning: could not persist pairing — you will need to re-pair after restart: {e}') and log to stderr; same treatment for the swallowed update_bots at telegram.rs:714.
- **Verifier:** telegram.rs:530-539 exact: persist_owner swallows cli_config::save at 538. Caller verified at 639: pairing pushes chat into the IN-MEMORY allowlist (638) and sends '✅ Paired — you're the owner.' (641-643) regardless of persistence — so pairing works this session and silently evaporates on restart, locking the owner out. Second swallowed write confirmed at 714-717 (do_remove_bot: let _ = store::update_bots — a removed bot silently resurrects at restart). No stderr log on either path.

### VIS-02 [visibility-surfaces] MCP server connect failures are reported only via eprintln!, which the retained TUI wipes; a failed server then vanishes from /mcp

- **Vị trí:** `src/agent/mcp.rs:1543` · verdict CONFIRMED
- **Hệ quả:** A configured MCP server that crashes or times out at connect leaves its tools silently missing: the stderr notice is painted over by the next ratatui frame (the exact failure class recorded in the user's own memory note about println under the retained TUI), and /mcp shows a healthy-looking list without the dead server or its error. The user cannot tell why the agent 'doesn't have' a tool they configured.
- **Bằng chứng:** eprintln!("{}", console::style(format!("mcp: server '{name}' skipped — {e:#}")).yellow()) — also line 1522 "mcp: connecting {} server(s)…" and line 1545. ensure_manager() runs during turn-start registry construction, i.e. while the retained alt-screen renderer owns the terminal; summary() (line 1727) then iterates only mgr.servers, so a server that failed to connect appears in NO surface when at least one other server connected (only the all-failed case gets the blanket line 1723).
- **Hướng sửa:** Route the connect-phase messages through tui::emit_line (like every other in-REPL notice), and keep failed servers in the Manager as a Failed{name, error} entry so summary() renders "✗ <name> — skipped: <error>" alongside the connected ones. That gives /mcp a complete picture and makes the failure visible the moment it happens.
- **Verifier:** mcp.rs:1543 eprintln! 'mcp: server '{name}' skipped — {e:#}' verbatim; 1522 and 1545 also raw eprintln!. build_manager (1538-1550) pushes only successful handles into Manager.servers — failures are dropped after the eprintln. summary() iterates mgr.servers only (loop at 1727); the all-failed blanket is 1722-1723; /mcp renders summary() via tui::emit_line (main.rs:6351) so a partially-failed setup shows a healthy-looking list. ensure_manager (1562-1565) returns early once built, so the error never re-prints. No stderr bridge exists: tui.rs:784-790 (emit_trace_line) is the codebase's own rule that eprintln is only for when the TUI does NOT own the screen, and connect runs during turn-start registry build (discovered_tools→ensure_manager, mcp.rs:1653-1654) with the retained TUI active. P1 stands.

### VIS-03 [visibility-surfaces] Per-turn autosave swallows write errors while /help promises 'auto-saves as you go'

- **Vị trí:** `src/main.rs:6752` · verdict CONFIRMED
- **Hệ quả:** If sessions_dir is unwritable (disk full, permissions, OneDrive lock), every turn's autosave fails silently — the exit-flush handler (flush_live_session_on_exit → autosave_last) fails the same way — and the user discovers the entire conversation history is gone only at the next launch, after having been promised continuous autosave. A promised behavior silently no-ops with data loss as the consequence.
- **Bằng chứng:** let _ = save_session(history, &name); (and line 6754: let _ = save_session(history, "last");) versus the /help promise at src/main.rs:5716: "/sessions          saved conversations — restore · save · delete (auto-saves as you go)".
- **Hướng sửa:** On the first save_session error per session, emit_line a warning ("⚠ autosave failed: <err> — conversation is NOT being persisted") and flip the footer health chip or add a status_text chip (e.g. "⚠ no autosave") until a save succeeds. Rate-limit to once so it doesn't spam every turn.
- **Verifier:** main.rs:6752 'let _ = save_session(history, &name);' and 6754 'let _ = save_session(history, "last");' verbatim inside autosave_last; save_session (6524-6536) returns Result with full context (create_dir_all, atomic_write, harden) but no caller on the autosave path surfaces it; flush_live_session_on_exit (6671-6674) funnels into the same swallow. /help promise verbatim at main.rs:5716 '(auto-saves as you go)'. No warning, chip, or rate-limited notice exists anywhere. P1 stands.

### VIS-04 [visibility-surfaces] Startup 'last conversation' offer and /resume are cross-project — the global flat sessions pool carries no project identity

- **Vị trí:** `src/main.rs:3754` · verdict CONFIRMED
- **Hệ quả:** Launching aizen in project B greets the user with project A's conversation as "last conversation" with no project label; /resume then loads it and refresh_dynamic_prompt_lane (load_session, line 6543) splices THIS project's context under the foreign transcript — a silently mixed-context thread. The user is misled about which project the offered conversation belongs to and the agent may act on wrong-repo assumptions.
- **Bằng chứng:** "⟲ last conversation “{}” ({n} messages) — /resume to continue it" — fed by most_recent_session() which reads the global sessions_dir()/last.json (src/main.rs:6576, sessions_dir at 6464 = ~/.aizen/sessions). Observed in C:/Users/admin/.aizen/sessions/: sessions from different projects mixed in one pool (hãy-vào-thư-mục-aizenbe-0726.json, vào-thư-mục-aizenweb-rồi-0723.json, cusersadmindesktopcauut-...json) with a single shared last.json.
- **Hướng sửa:** Stamp each session file with the project slug (a small wrapper struct or a sidecar field) at save time; scope most_recent_session() to the current slug by default and label foreign offers explicitly ("last conversation here …" vs "… in aizen-web"). Show the origin project in the /sessions picker rows.
- **Verifier:** main.rs:3754 quote verbatim; most_recent_session (6575-6588) reads the global sessions_dir()/last.json (read at 6576); load_session (6537-6546) calls refresh_dynamic_prompt_lane at 6543, splicing the current project's context under the foreign transcript. Session files are bare Vec<Message> JSON — save_session (6524-6536) writes serde_json::to_vec_pretty(history) with no project field. Observed pool in ~/.aizen/sessions/: 13 files from at least 3 projects (hãy-vào-thư-mục-aizenbe-0726.json, vào-thư-mục-aizenweb-rồi-0723.json, cusersadmindesktopcauut-…0725.json) sharing one last.json. No project label on the offer, /resume, or the /sessions picker rows (6773-6788). P1 stands.

### F1 [config-dead-state] Recovery leases leak forever: 115 dirs / 22 MB of conversation history invisible, un-discardable, never GC'd

- **Vị trí:** `src/core/recovery.rs:288` · verdict CONFIRMED
- **Hệ quả:** Every crash, kill, or closed terminal permanently deposits the user's full conversation transcript on disk. The user is never told these exist, no command lists them, and even '/recover discard' cannot remove most of them — silent unbounded growth of private data the user believes was cleaned up ('Clean exit: drop the lease' is the module's own promise).
- **Bằng chứng:** if manifest.sidecar_generation == 0 || manifest.safe_history_len == 0 {
    continue;
}" — scan_stale() silently SKIPS (never deletes) gen-0 leases, and line 285 skips any lease whose repo_scope differs from the cwd. Observed: C:/Users/admin/.aizen/recovery/ holds 115 lease dirs (22 MB), 56 with no history sidecar (gen-0, unreachable by any command) and 59 containing full conversation history.*.json (one has history.61.json). recovery::clear() runs only on the graceful-exit path (src/main.rs:4183); /recover discard (main.rs:6031) iterates only scan_stale()'s filtered offers, so gen-0 and foreign-directory leases can never be removed.
- **Hướng sửa:** Add age-based GC to scan_stale (or begin()): delete any lease dir whose manifest is unparseable, gen-0, or older than N days regardless of repo_scope, and delete quarantine-*/consumed-* remnants. Extend '/recover discard --all' to sweep every scope. Surface a one-line count ('N stale recovery snapshots, M KB — /recover discard') at startup so the state is visible.
- **Verifier:** Quote exact at src/core/recovery.rs:288; lines 285-287 skip foreign repo_scope. begin() (recovery.rs:117-146) writes sidecar_generation:0 on EVERY launch, so any non-graceful exit strands a gen-0 dir. clear() has exactly one call site (src/main.rs:4183, graceful exit); '/recover discard' (main.rs:6030-6034) iterates only scan_stale() offers and discard() (recovery.rs:345) requires an offer, so gen-0 and foreign-scope leases are permanently unremovable. No age-based GC anywhere in recovery.rs (grep: no elapsed/days logic; quarantine() only renames). Live data re-measured: 115 lease dirs, 21.4 MB, 56 with no history sidecar, 59 with full conversation history. P1 fits: '/recover discard' prints 'discarded N recovery lease(s)' while leaving 100+ transcript-bearing dirs, and no surface ever discloses them.

### F2 [config-dead-state] User-facing global rules file ~/.aizen/rules.md ('apply to ALL projects') is read by nothing

- **Vị trí:** `C:/Users/admin/.aizen/rules.md:2` · verdict CONFIRMED
- **Hệ quả:** The file explicitly promises 'these rules apply to ALL projects' and contains real user preferences (respond in Vietnamese, be concise). The current binary never loads it — the user's written instructions silently no-op, and any future edit to this file will also do nothing while looking authoritative.
- **Bằng chứng:** "# Global NextGen Rules\n# These rules apply to ALL projects.\n## Language\n- Respond in Vietnamese" — grep across src/ for 'rules.md' (or any nextgen_home().join("rules...")) returns zero readers and zero writers; no creator in current tree.
- **Hướng sửa:** Either honor it (inject rules.md into the stable prompt lane next to STYLE.md/SOUL.md) or delete-and-migrate: on startup, detect a non-empty ~/.aizen/rules.md, tell the user it is no longer read, and offer to import its lines into cli-memory STYLE/global prefs before renaming it to rules.md.migrated.
- **Verifier:** File exists (154 bytes, last written 2026-04-03 — predates the 2026-05-08 v0.1.0 scaffold, and contains the user's real preferences matching their known profile). Content matches the quote verbatim. Grep across the entire repo (src + docs + README) for 'rules.md' and join("rules returns zero hits — no reader, no writer, no migration. The header's promise 'apply to ALL projects' silently no-ops: P1 correct.

### F3 [config-dead-state] Decoy config files: ~/.aizen/mcp/servers.json and ~/.aizen/settings.json look editable but are dead; real stores are mcp.json and cli-config.json

- **Vị trí:** `src/agent/mcp.rs:95` · verdict CONFIRMED
- **Hệ quả:** A user (or another tool) who registers an MCP server in mcp/servers.json or sets a key/baseURL in settings.json gets zero effect and zero error — the classic silently-ignored-config trap. Two parallel 'mcp' stores (mcp.json vs mcp/) in the same directory guarantee the wrong one gets edited eventually.
- **Bằng chứng:** crate::core::config::nextgen_home().join("mcp.json") — the ONLY MCP server store the code reads. Yet ~/.aizen/mcp/servers.json exists containing '{"schemaVersion": 1, "servers": []}' (an obvious 'add your server here' shape) and ~/.aizen/settings.json contains '"provider": { "apiKey": "", "provider": "proxy-api", "baseURL": "http://localhost:5217" }' — grep across src/ finds zero readers of either file (only recovery.rs matches 'manifest.json'). No creator in current tree (v0.1.0 scaffold, all dated 2026-05-08).
- **Hướng sửa:** On startup (or in `aizen doctor`), detect known-dead scaffold files; if mcp/servers.json has a non-empty servers array or settings.json has a non-empty provider block, warn loudly and point at the live file. Then delete or rename the empty dead scaffolds (see F9) so only real, read files remain under ~/.aizen.
- **Verifier:** src/agent/mcp.rs:94-95 confirmed as the only home MCP store (nextgen_home().join("mcp.json")); project store is <root>/.nextgen/mcp.json (:101-103). Both decoys verified live: ~/.aizen/mcp/servers.json = {schemaVersion:1, servers:[]} and ~/.aizen/settings.json has the provider block with apiKey/baseURL (plus hooks.enabled:true, mcp.enabled:false — more dead promises), both stamped 2026-05-08. Grep across src/ for servers.json|settings.json: zero matches. The real ~/.aizen/mcp.json exists alongside, so the two-parallel-stores trap is real. P1 correct.

### EXT-3 [external-deps] No central git resolver: 15 scattered Command::new("git") sites, zero PATH fallback, zero one-time warning — while the repo already contains the exact resolver needed

- **Vị trí:** `src/agent/lsp/discovery.rs:125` · verdict ADJUSTED
- **Hệ quả:** Six subsystems (project identity, time machine, workspace locking, repo map, self-review, agents-install clone) each degrade differently and silently when the same single binary is absent; the user with git installed at C:/Program Files/Git/cmd/git.exe but not on PATH gets ALL degradations despite git being 1 directory-probe away.
- **Bằng chứng:** pub fn resolve_server_binary(spec: &ServerSpec) -> Result<PathBuf> { if let Some(p) = which_on_path(spec.command) { return Ok(p); } … Err(anyhow!("language server '{}' not found on PATH — install it to enable LSP for {} (the agent falls back to text search)")) — LSP binaries get PATH+PATHEXT resolution and an explicit degradation message; git (config.rs:65, config.rs:178, timemachine.rs:527/568/810/849/986/1024/1065/1211, main.rs:8781, workspace_txn.rs:310, agent/mod.rs:2976, repo_map.rs:170) gets bare Command::new("git") everywhere, re-probed per call, and no such message exists (grep for "git not found"/"git.exe"/"Program Files.*Git" in src/: zero hits outside one test string).
- **Hướng sửa:** Add src/core/gitbin.rs: `static GIT: OnceLock<Option<PathBuf>>`; `pub fn resolve_git() -> Option<&'static Path>` probing once: (1) AIZEN_GIT env override, (2) which_on_path("git") — hoist lsp::discovery::which_on_path/search_dirs to core (already PATHEXT-correct), (3) known locations: `C:\Program Files\Git\cmd\git.exe`, `C:\Program Files (x86)\Git\cmd\git.exe`, `%LOCALAPPDATA%\Programs\Git\cmd\git.exe`, `/usr/bin/git`, `/usr/local/bin/git`, `/opt/homebrew/bin/git`. `pub fn git_cmd() -> Option<Command>` returns a Command pre-pointed at the resolved absolute path. On first None, emit exactly one visible `tui::emit_line` warning naming every degraded feature, and expose the resolved path (or MISSING) in /status and `aizen time doctor`. Convert all 15 call sites.
- **Verifier:** Count correction: exactly 14 bare Command::new("git") sites, not 15 — the finding's own list (config.rs:65,178; timemachine.rs:527,568,810,849,986,1024,1065,1211; main.rs:8781; workspace_txn.rs:310; agent/mod.rs:2976; repo_map.rs:170) is complete and grep-verified; no 15th exists (timemachine's git()/source_git() route through 527/568). Everything else confirmed: resolve_server_binary at lsp/discovery.rs:125-139 with PATHEXT-aware which_on_path (:146-160) is exactly the missing resolver; git is re-probed per call (project_root shells out on every invocation, no caching of git resolution); zero proactive 'git not found' warning anywhere (only per-error '(is git installed?)' contexts at timemachine.rs:559/577/990/1069 and main.rs:8785). Severity P1 stands.

### EXT-4 [external-deps] Self-review quality gate silently no-ops when git is unavailable

- **Vị trí:** `src/agent/mod.rs:2941` · verdict ADJUSTED
- **Hệ quả:** With enable_self_review on and edits made, the promised oracle/self review before Done simply never happens when git is off PATH — conflated with the legitimate "clean diff / LGTM" outcome. The user believes every edit run was reviewed; none were, and no trace line distinguishes "reviewed: clean" from "review skipped: no git".
- **Bằng chứng:** let diff = git_diff_capped()?; — git_diff_capped (line 2976) is `std::process::Command::new("git").args(["diff"]).output().ok()?` so binary-not-found returns None, and oracle_review returns None, documented at 2928 as "`None` ⇒ nothing actionable (no git / empty diff / LGTM / call failed)"
- **Hướng sửa:** Split the None cases: when resolve_git() is None or `git diff` errors, emit_trace "self-review skipped: git unavailable — diff could not be produced" (once per session via the central resolver's warning); optionally fall back to reviewing the turn's collected edit results (the loop already knows which tools edited what) instead of the working-tree diff.
- **Verifier:** Code evidence exact: mod.rs:2941 'let diff = git_diff_capped()?;', :2976 '.output().ok()?', doc :2928 conflates 'no git / empty diff / LGTM / call failed', and the None case falls through to Done with no trace (:1277 'else: LGTM / no diff'). Scenario correction: on the git-off-PATH machine the finding frames, this no-op is UNREACHABLE — EXT-1's fail-closed gate refuses every checkpoint-gated edit, and made_any_edits is set only by needs_checkpoint tools (turn_made_edits, mod.rs:2013-2037), so self-review never arms. The reachable silent no-op today is git PRESENT + non-repo directory (edits succeed via the benign path, `git diff` exits non-zero -> None) or any other git-diff failure — still 'user opted into self_review, believes runs are reviewed, none are, indistinguishable from LGTM'. P1 stands with the corrected trigger; also becomes live for git-missing the moment EXT-1 is fixed benign.

### EXT-5 [external-deps] is_repo() conflates "git binary missing" with "not a repo" — checkpoint tools tell the user to run `git init`, which also cannot run

- **Vị trí:** `src/features/timemachine.rs:1324` · verdict CONFIRMED
- **Hệ quả:** In a real git repo with git off PATH, the model and the user are told the directory is "not a git repository" and advised to `git init` — both claims are false/impossible ('git' is not recognized). The same collapse hides genuinely different failures (dubious ownership, corrupt private store) behind the identical message, sending the user down the wrong repair path.
- **Bằng chứng:** pub fn is_repo() -> bool { RepoContext::current().is_ok() } — used by every checkpoint tool, e.g. line 2190-2191: if !is_repo() { return Ok("error: not a git repository — checkpoints need git (run `git init`)".to_string()); }
- **Hướng sửa:** Make RepoContext::discover return a typed enum (NoWorkTree | GitMissing | GitError(String)) instead of substring-matched anyhow text; have is_repo()/checkpoint tools render distinct messages: "git is not installed or not on PATH (looked in PATH + C:\Program Files\Git\cmd) — install it or set AIZEN_GIT" vs the current `git init` hint, sourced from the central resolver state.
- **Verifier:** timemachine.rs:1324 'pub fn is_repo() -> bool { RepoContext::current().is_ok() }' collapses ALL discover failures (git missing: 'running git (is git installed?)'; dubious ownership; corrupt store) into false; 5 checkpoint tools (2190, 2245, 2308, 2369, 2486) then return 'error: not a git repository — checkpoints need git (run `git init`)' — verified verbatim at 2190-2191. In a real repo with git off PATH both claims are false and the advised command cannot run. Ironically discover's own comment (:389-394) says real errors should 'propagate with their true cause' — is_repo() defeats that. P1 right.

### EXT-6 [external-deps] MCP server connect failures go to raw eprintln! (wiped by the retained TUI) and are not recorded — a failed server vanishes without a queryable trace

- **Vị trí:** `src/agent/mcp.rs:1543` · verdict CONFIRMED
- **Hệ quả:** build_manager runs lazily during the first turn's registry build — i.e. while the retained TUI owns the screen — so the only line naming WHICH server failed and WHY (bad token, missing npx, crash — the stderr tail the code carefully captured) is painted over by the next frame. Afterwards /mcp lists only successes; a configured-but-failed server and its reason are unrecoverable without restarting with a scrollback capture.
- **Bằng chứng:** eprintln!("{}", console::style(format!("mcp: server '{name}' skipped — {e:#}")).yellow()) — while the correct channel exists (tui.rs:753 `if retained::is_running()` routes emit() into the render-thread buffer); failed servers are dropped from Manager (only `servers.push(h)` on Ok at 1541), so summary() line 1723 can only say "No MCP servers connected (all disabled or failed to connect)."
- **Hướng sửa:** Replace the eprintln! calls in build_manager/ensure_manager (lines 1522, 1543, 1545, 1575) with crate::ui::tui::emit_line, and store failures in Manager as `failed: Vec<(String, String)>` (name, reason with stderr tail, secrets masked) so summary()//mcp renders "✗ name — skipped: reason" rows alongside the connected servers.
- **Verifier:** All evidence verified: eprintln! at mcp.rs:1522, 1543, 1545, 1575; failed servers dropped (only 'servers.push(h)' on Ok at 1541, Manager{servers,generation} at 1550 has no failure field); summary() at 1717-1723 can only say 'No MCP servers connected (all disabled or failed to connect)' and /mcp (main.rs:6351, 955, 970) renders it. Correct channel exists and is documented: tui.rs:740-763 emit() routes via retained::is_running() specifically because raw prints 'would be wiped by resume's clear+redraw'. Lazy first-turn timing confirmed: discovered_tools() (mcp.rs:1653-1654) -> ensure_manager -> block(build_manager) during registry build, i.e. while the retained TUI owns the screen. The carefully-captured stderr tail (name + reason) is painted over and unrecoverable. P1 right.

### EXT-8 [external-deps] hostbot root service install prints '✓ enabled + started' even when systemctl failed

- **Vị trí:** `src/hostbot/service.rs:123` · verdict CONFIRMED
- **Hệ quả:** On a root install where systemctl is absent or errors (container without systemd, masked unit), the user is told the always-on bot is enabled and reboot-proof when nothing was started at all — active misinformation about a service whose whole point is 'stays alive'; the user only finds out when the bot goes dark.
- **Bằng chứng:** let _ = std::process::Command::new("systemctl").args(["enable", "--now", "aizen-serve"]).status();
                println!("{}", style("✓ enabled + started (auto-starts on reboot)").color256(splash::ACCENT)); — contrast the --user branch (lines 98-108) which checks `st` and prints a yellow failure hint
- **Hướng sửa:** Mirror the --user branch: capture `.status()` of the enable --now call and print the success line only on `s.success()`, else the yellow "wrote the unit, but `systemctl enable --now` failed — run it manually" line (and do the same for the daemon-reload result on line 122).
- **Verifier:** service.rs:122-124 verified: root branch discards both statuses ('let _ = ... daemon-reload', 'let _ = ... enable --now') and unconditionally prints '✓ enabled + started (auto-starts on reboot)'. Contrast confirmed: the --user branch (98-109) captures st and prints the yellow 'wrote the unit, but `systemctl --user enable --now` failed — run it manually.' on failure. Cited line 123 is the discarded enable call; the unconditional success print is line 124. Active misinformation about an always-on service; P1 right (narrow path: root install on systemd Linux).

### ERR-01 [error-quality] Mid-stream failure discards the partial answer from history while it stays rendered on screen as a normal reply — and the user's question is popped too

- **Vị trí:** `src/llm/client.rs:1065` · verdict CONFIRMED
- **Hệ quả:** After a stall/drop mid-answer the transcript shows the user's question and half an assistant reply that look completely normal, but NEITHER exists in the conversation any more. The user types "continue" and the model has no idea what they mean; nothing tells them the visible partial was thrown away or that they must re-send their message. Screen state and model state actively disagree.
- **Bằng chứng:** client.rs:1064-1069: "// The display is now clean … — surface any transport error // that interrupted the stream. The partial `full` / tool-calls are intentionally discarded" → `if let Some(e) = stream_err { return Err(e); }`. The retained renderer only adds a newline on interrupt (src/ui/tui/retained.rs:449-459: `fn finish_assistant(&mut self, interrupted: bool) { … block.complete = true; if interrupted { … s.push('\n');`) — no visual marker. Then main.rs:4161-4165: `Some(Err(e)) => { tui::emit_line(&format!("{} {e}", theme::err("error:"))); if history.last().map(|m| m.role == "user").unwrap_or(false) { history.pop(); } }`
- **Hướng sửa:** When `stream_err.is_some()` and `produced`, render an explicit marker under the partial block (e.g. "⚠ stream interrupted — the partial answer above was discarded; the model will not remember it"), and either keep the partial text in history tagged as interrupted (text-only case; the tool-call case can stay discarded) or at minimum restore the popped user message into the input draft so one keypress re-sends it. `finish_assistant(interrupted=true)` should style the block (dim/strike or a trailing ⏸ badge) instead of just appending a newline.
- **Verifier:** All three evidence sites reproduced exactly: client.rs:1064-1070 discards partial `full`/tool-calls and returns Err; retained.rs:449-459 finish_assistant(interrupted) only appends a newline (block rendered as a normal complete reply); main.rs:4161-4165 pops the user message. Attempted refutations failed: the blank-stream replay (client.rs:1042-1062) is gated on !produced so a mid-answer failure never retries; the 'restored interrupted draft' path (main.rs:6046) is startup crash-recovery only, nothing restores the popped message in-session. One mitigation exists — the REPL does print 'error: {e}' — but nothing marks the on-screen partial as discarded, so screen and model state still disagree. P1 right (the error line keeps it out of P0).

### ERR-02 [error-quality] run_tool_body formats tool errors with `{e}` — the anyhow cause chain is dropped, so a failed sub-agent still reads as the bare "error: sub-agent (coder) failed"

- **Vị trí:** `src/agent/mod.rs:2179` · verdict CONFIRMED
- **Hệ quả:** When a sub-agent dies after its 4 transient retries (e.g. "upstream returned HTTP 429 …" or "SSE stream error: timeout — no data for 90s"), the parent's tool result — the ONLY string the model and the user ever see — is exactly "error: sub-agent (coder) failed" with zero cause. This is the same bare string the retry work was supposed to fix (memory note subagent-quits-on-big-tasks); the cause was attached to the chain but the formatter amputates it. The same `{e}` also strips `.context()` chains from every other tool: a tool that wraps an io error in a friendly context loses the OS reason, and one that doesn't loses the path.
- **Bằng chứng:** mod.rs:2177-2180: `let out = match tool.execute(args) { Ok(out) => out, Err(e) => format!("error: {e}"), };` — `{e}` prints only the OUTERMOST anyhow context. task_tool.rs:579-581 deliberately chains the real cause underneath: `Err(e) => { track.finish_err(format!("error: {e}")); return Err(e).with_context(|| format!("sub-agent ({label}) failed")); }`
- **Hướng sửa:** Change line 2179 to `format!("error: {e:#}")` (anyhow alternate = full "outer: inner: root" chain, still one line). That single character fixes the sub-agent path, the checkpoint-failure path (which already uses `{e:#}` when building its message), and every `.context()`-disciplined tool at once. Optionally cap the chain rendering length before it enters the tool result.
- **Verifier:** mod.rs:2179 is exactly `Err(e) => format!("error: {e}")` — anyhow Display prints only the outermost context, so task_tool.rs:581's `.with_context(|| format!("sub-agent ({label}) failed"))` yields the bare string with the real cause (429/stall/timeout) amputated for every consumer (model, transcript digest, and track.finish_err at 580 gets the same). Refutation attempted: mod.rs already uses `{e:#}` at 1558 and 1968, proving the chain-preserving formatter is the codebase's own convention and this site is the outlier. No other formatter intercepts tool.execute errors. P1 right.

### ERR-03 [error-quality] Per-turn autosave and the window-close flush swallow every write error — the promised "auto-saved as last after every turn" can silently no-op forever

- **Vị trí:** `src/main.rs:6752` · verdict CONFIRMED
- **Hệ quả:** If ~/.aizen/sessions becomes unwritable (disk full, ACL, Windows Controlled Folder Access blocking the binary — common), every turn's autosave fails silently, the ✕-close flush fails silently, and the user discovers only after closing the window that the whole conversation was never persisted. This is data loss with zero warning, contradicting an explicitly promised behavior.
- **Bằng chứng:** main.rs:6752-6754 in `fn autosave_last`: `let _ = save_session(history, &name); if name != "last" { let _ = save_session(history, "last"); // keep `last` as the "most recent" pointer }` — and the doc comment at 6766-6767 promises "The live chat is also auto-saved as `last` after every turn, so there's always something to come back to." The exit path (main.rs:6671-6674 `flush_live_session_on_exit` → `autosave_last`) inherits the same swallow.
- **Hướng sửa:** Surface the first failure once per session: `if let Err(e) = save_session(...) { tui::emit_line(&format!("⚠ autosave failed ({e:#}) — this conversation is NOT being saved")); }` guarded by a `AUTOSAVE_WARNED` AtomicBool so it doesn't spam every turn. Also flip the footer health chip (set_health) to warn state while autosave is failing.
- **Verifier:** main.rs:6752-6754 swallows both save_session results with `let _ =` (plus a third swallow of checkpoint_history at 6757); the promise 'auto-saved as last after every turn' is verbatim at 6766-6767; flush_live_session_on_exit (6671-6674) and the Windows ✕ handler (6704-6708) inherit the swallow. Refutations checked: the manual /sessions save path DOES print errors (6829), and the REPL prints 'recovery checkpoint not updated' (4174) — but that covers the recovery file, not the sessions dir, so a sessions-only write failure (ACL/CFA/lock on last.json) is silent every turn. P1 right per the rubric's own 'promised behavior silently no-ops' definition.

### ERR-04 [error-quality] Barrier-path failures (pre-edit checkpoint failed, writer lease not acquired, external-path refusal, unknown tool, bad args, denial) are fed to the model but never rendered — the tool call is invisible in the transcript

- **Vị trí:** `src/agent/mod.rs:1967` · verdict CONFIRMED
- **Hệ quả:** The user watches the agent "work" while an entire class of consequential refusals happens off-screen: an edit that was blocked because Time Machine couldn't checkpoint, a workspace lease timeout, a denied action's resolution. The model silently reroutes or retries and the user never learns their protection layer fired or why the agent changed course. Even the approval prompt is left dangling: after [n], no line ever records that the call was declined and skipped.
- **Bằng chứng:** mod.rs:1967-1969: `Err(e) => Some(format!("error: protected workspace change was not run because the pre-edit checkpoint failed: {e:#}"))` — this string (and the lease error at 1925-1927, the External refusal at 1939-1943, `format!("error: unknown tool '{}'"` at 1900, parse errors at 1898, and the denial string) flows only into `land(i, out, &mut results, sink)` at 1997. `emit_tool_call`/`emit_tool_result` live inside `run_tool_body` (2175/2183), which never executes on these branches — no transcript line is ever emitted for the call.
- **Hướng sửa:** In the barrier arm, when `out` is produced without running `run_tool_body` (denial, lease/checkpoint/External/unknown/parse errors), emit the standard pair: `let seq = emit_tool_call(name, args); emit_tool_result(seq, name, args, &out, None);` (respecting cfg.quiet). That reuses the existing 'error:' red digest styling and makes every refused call a visible row, resolving the dangling approval prompt for free.
- **Verifier:** Verified end-to-end: `land` is a pure closure (mod.rs:1813-1816, sink+results only); the deny (1902/2154), lease error (1925-1927), External refusal (1939-1943), checkpoint failure (1967-1969), unknown tool (1900) and parse error (1898) all reach land(1997) without ever entering run_tool_body, where the only emit_tool_call/emit_tool_result sites live (2175/2183). ask_approval (tui.rs:552-576) emits the prompt but returns silently on [n] — nothing records the resolution. The finding correctly excludes cmd_guard Blocked/Caution, which DO emit (2112-2143). P1 right: the Time Machine/lease protection layer fires invisibly and the model reroutes with the user unaware.

### ERR-05 [error-quality] A network-level failure reaches the REPL as exactly "error: request failed after retries" — the reqwest cause (DNS/refused/TLS/proxy, and the URL it holds) is in the dropped chain

- **Vị trí:** `src/llm/client.rs:370` · verdict CONFIRMED
- **Hệ quả:** Wrong base_url, dead proxy, DNS failure, TLS interception, offline — all collapse to the identical 11-word string. The user cannot distinguish "my key config points at a typo'd host" from "my wifi is down" from "the provider is down", which is precisely the moment they most need the WHY.
- **Bằng chứng:** client.rs:370: `return Err(anyhow!(e)).context("request failed after retries");` — the reqwest error (which names the failing URL and kind) becomes the chained SOURCE. Every REPL render site prints Display only: main.rs:4162 `tui::emit_line(&format!("{} {e}", theme::err("error:")))` (same `{e}` at 3919, 4309, 4417).
- **Hướng sửa:** Either format the render sites with `{e:#}` (one-line chain: "request failed after retries: error sending request for url (https://…): dns error: …"), or enrich the context at the source: `.with_context(|| format!("request to {url} failed after retries"))` by moving `send_with_retry` to take the url for the message. Doing both is cheap and covers 401 bodies too.
- **Verifier:** client.rs:370 exactly `return Err(anyhow!(e)).context("request failed after retries")` — the reqwest error (kind + URL) becomes a chained source that anyhow Display drops. All four REPL render sites print `{e}` only (main.rs:3919, 4162, 4309, 4417 — verified by grep). Refutation attempted: no intermediate layer re-wraps chat() errors with endpoint info, and classify_api_error (396-403) also reads only e.to_string() so a transport error is classified from the bare 11-word string. Severity sits on the P1/P2 line but P1 is defensible and consistent with ERR-02 (same amputated-chain class, zero actionable residue).

### ERR-06 [error-quality] HTTP errors never name the endpoint or model, and dump the unbounded raw response body

- **Vị trí:** `src/llm/client.rs:361` · verdict CONFIRMED
- **Hệ quả:** aizen supports multiple providers/gateways plus sub-agents and post-turn passes that may hit different models. On a 401 the user reads "upstream returned HTTP 401 Unauthorized: …" with no clue WHICH endpoint rejected WHICH key — they rotate the wrong key. And a gateway 502 whose body is a full Cloudflare HTML page injects kilobytes of markup into a single transcript line (it also becomes the string classify_api_error scans).
- **Bằng chứng:** client.rs:360-361: `let detail = resp.text().await.unwrap_or_default(); bail!("upstream returned HTTP {status}: {detail}");` — no base_url, no model, no truncation of `detail`. `send_chat` (453-486) has both `url` and `body.model` in scope but passes neither into the error.
- **Hướng sửa:** In `send_chat`, wrap: `send_with_retry(...).await.with_context(|| format!("{} (model {})", redacted_endpoint(url), body.model))` where redacted_endpoint keeps scheme+host+path. Truncate `detail` to ~500 chars at line 360 (`detail.chars().take(500)`), preserving the leading JSON error message providers put first.
- **Verifier:** client.rs:360-361 exactly as quoted: no endpoint, no model, no truncation of `detail` (a Cloudflare 502 HTML page enters the error string whole). send_chat (453-486) demonstrably has `url` and `body.model` in scope — it even uses body.model in the reasoning_effort fallback note at 471-474 — and passes neither into send_with_retry's error. Refutation attempted: no truncation or endpoint-attribution exists anywhere between bail! and the render sites; body_blames_effort/classify_api_error scan the unbounded string. P1 defensible: on multi-provider setups a 401 misdirects the user to rotate the wrong key.

### ERR-07 [error-quality] Interactive REPL never tells the user WHY a turn was force-stopped: Divergence, MaxIters and VerificationFailed end silently (one-shot mode does surface them)

- **Vị trí:** `src/main.rs:4091` · verdict CONFIRMED
- **Hệ quả:** When the loop guard hard-stops a diverging turn (agent/mod.rs:1470-1474 returns with possibly empty `turn.content`) or the step cap/verify gate ends it, the REPL turn just… ends. The user cannot tell "the agent decided it was done" from "the harness killed a loop" from "edits were made but verification never passed" — the last one meaning their tree may be broken. The exact strings already exist for one-shot mode and are simply not wired into the REPL.
- **Bằng chứng:** main.rs:4091 `Some(Ok(outcome)) => {` — the arm handles only goal-Done (4096-4107) and the empty-reply warning gated `if empty && outcome.iters <= 1` (4115); `outcome.stop` is otherwise never examined. Contrast the one-shot path, main.rs:8096-8098: `StopReason::Divergence => eprintln!("\n[stopped after {} steps: the model repeated the same tool call without progress]", outcome.iters)` (MaxIters/VerificationFailed likewise at 8100-8107).
- **Hướng sửa:** In the `Some(Ok(outcome))` arm, add a match on `outcome.stop` mirroring main.rs:8096-8107 via `tui::emit_line` with theme::warn — e.g. "⚠ stopped: hit the step limit (25 steps) — the task may be incomplete" and "⚠ stopped: edits were made but verification never passed". Three lines of code, strings already written.
- **Verifier:** REPL arm main.rs:4091-4160 examines outcome.stop only via the goal-Done matches! (4096-4098); Divergence/MaxIters/VerificationFailed fall through with no output (the empty-reply warning at 4115 is gated iters<=1, so a diverged turn at iter 6 with empty content shows literally nothing). One-shot strings verified verbatim at main.rs:8093-8107. Refutation attempted: the agent loop emits nothing at any of the three returns (mod.rs:1231, 1470-1474, 1601-1605, 1753) — the divergence nudge (1478) and the MaxIters synthesis prompt (1740-1744) are model-facing messages only, so a harness-guaranteed surface does not exist in the REPL. P1 right; the fix is genuinely three prewritten strings.

### SESS-04 [session-integrity] The last.json pointer is exposed as a first-class session: restoring "last" (picker or twin-miss fallback) makes the live conversation exist only in last.json, where any other conversation's next autosave silently destroys it

- **Vị trí:** `src/main.rs:6588` · verdict CONFIRMED
- **Hệ quả:** A user who picks "last" from /sessions (it looks like the newest chat) continues a conversation whose ONLY on-disk copy is last.json; the moment any other conversation autosaves (other instance, or a /clear + new chat), line 6754 overwrites last.json and everything typed since the restore is gone with no warning. Deleting "last" from the picker also silently breaks bare /resume and the startup hint (most_recent_session returns None at 6576) even though named sessions still exist.
- **Bằng chứng:** `Some((named.unwrap_or_else(|| "last".to_string()), last.len()))` (6588) hands "last" to /resume when no byte-identical twin exists; list_sessions (6607-6621) has no filter, so the /sessions picker and delete menu list "last — N msgs" as a normal session; load_session then pins it: `set_session_slug(Some(sanitize_name(name)));` (6545); autosave_last skips the pointer copy for it: `if name != "last" { let _ = save_session(history, "last"); }` (6753-6754).
- **Hướng sửa:** Exclude "last" from list_sessions (or render it as "(auto: most recent pointer)" and re-point restores to the resolved twin), never allow the live slug to become "last" (on twin-miss, copy last.json to a freshly allocated slug before restoring), and make bare /resume fall back to the newest named session when last.json is missing or corrupt.
- **Verifier:** All four mechanisms verified: twin-miss fallback hands 'last' as a restorable slug (6588); list_sessions (6607-6621) has no filter so the picker (6771-6783) and delete menu (6832-6839) show 'last' as a normal session; load_session pins the live slug to 'last' (6545); autosave_last skips the pointer copy when name == 'last' (6753-6754), leaving last.json as the conversation's ONLY on-disk copy, which line 6754 from any other conversation (other instance, or same instance after /clear allocates a new slug) silently overwrites. Deleting 'last' makes most_recent_session return None (6576 .ok()?) until the next autosave recreates it. P1 reasonable (conditional trigger), though the loss itself is real data loss.

### SESS-05 [session-integrity] "Save as" silently overwrites an existing session, and sanitize_name folds distinct names into one file (punctuation → '_', hard 80-char truncation) with no collision or overwrite warning

- **Vị trí:** `src/main.rs:6821` · verdict CONFIRMED
- **Hệ quả:** Saving a new conversation under a name that sanitizes to an existing file replaces that session's entire transcript instantly and silently — the UI prints "saved '<name>'" as if nothing was lost. The only overwrite guard in the codebase (allocate_session_slug's -2 suffix probing, 6639-6652) covers only auto-named chats, not the manual path.
- **Bằng chứng:** sessions_menu save path (6820-6827): `match save_session(history, name.trim()) { Ok(_) => { set_session_slug(...)` — no existence check, no Confirm (delete DOES confirm at 6841-6845). sanitize_name (6468-6472): `.map(|c| if c.is_alphanumeric() || c == '-' || c == '_' { c } else { '_' }).take(80)` — e.g. "fix bug!" and "fix bug?" both become fix_bug_, and any two names sharing an 80-char prefix collide.
- **Hướng sửa:** In the Save flow, when sanitize_name(name).json already exists and is not the current slug, show the sanitized filename and ask "overwrite existing session '<slug>' (N msgs)?" defaulting to No; also display the sanitized slug when it differs from what the user typed, so the mapping is explicit.
- **Verifier:** Save path (6813-6831): save_session called at 6821 with no existence check and no Confirm, then 'saved <name>' printed at 6827 — while the delete path DOES confirm (6841-6845). sanitize_name (6467-6489) maps every non-alphanumeric/-/_ char to '_' and truncates at 80 chars (6470-6471), so distinct typed names collide onto one file. The only collision guard, allocate_session_slug's -N suffix probing (6639-6652), applies exclusively to auto-named chats. P1 correct.

### SESS-06 [session-integrity] Closing the terminal window (or SIGHUP/SIGTERM) never clears the recovery lease, so every normal window-close fakes a crash: 115 stale lease dirs (21.4 MB) observed, a perpetual "recoverable session" banner, and the more useful /resume hint suppressed

- **Vị trí:** `src/main.rs:6707` · verdict CONFIRMED
- **Hệ quả:** Users who exit by closing the window (a normal exit the flush handler explicitly supports) are greeted next launch with "⟳ recoverable session · phase=idle …" implying a crash that never happened, while the accurate "⟲ last conversation … /resume" hint is hidden. Leases whose repo_scope never matches again (recovery.rs:285-287 `if manifest.repo_scope != repo_scope { continue; }`) are invisible on every surface and are never garbage-collected — unbounded growth of dirs containing full conversation histories. /recover also restores only offers[0] without saying how many others exist, and never sets the session slug, silently forking the recovered chat into a new file.
- **Bằng chứng:** Windows console handler (6704-6709) does only `let _ = std::panic::catch_unwind(flush_live_session_on_exit);` and the unix handler (6725-6731) does flush + `std::process::exit(0)` — `crate::core::recovery::clear()` runs only on the graceful loop exit (4183). Observed C:/Users/admin/.aizen/recovery: 115 lease dirs since 2026-07-21, 21.4 MB, 59 with generation>0; 101 scoped to \\?\C:\Users\admin, 2 to \\?\C:\Windows\System32. Startup shows only the first offer (3743 `.into_iter().next()`) and its presence suppresses the /resume hint (3746 `else if`).
- **Hướng sửa:** Call recovery::clear() from the console-control/SIGHUP/SIGTERM handlers after the flush (the lease is redundant once the transcript is saved); add TTL-based GC of leases older than N days in scan_stale; when multiple offers match, say "N recoverable sessions — newest shown"; and give /recover a way to list/inspect leases from other scopes instead of hiding them forever.
- **Verifier:** Code verified: Windows handler (6704-6710) and unix handler (6725-6732) only flush; recovery::clear() has exactly one caller in the whole tree, the graceful loop exit at main.rs:4183. scan_stale (recovery.rs:256-310) skips foreign-scope leases at 285-287 and has no TTL/GC (quarantine renames only corrupt entries); /recover discard (6030-6034) only reaches in-scope offers, so foreign-scope leases are permanently invisible and undeletable from any surface. Startup shows only offers[0] (3743 .into_iter().next()) and suppresses the /resume hint via else-if (3746). Data re-measured today: 115 lease dirs, 22,410,638 bytes (21.4 MiB), oldest 21/07; manifests: 101 scoped \\?\C:\Users\admin, 2 \\?\C:\Windows\System32, 59 with sidecar_generation>0 — the auditor's numbers match exactly. format_offer (recovery.rs:379-388) renders 'recoverable session · phase=idle · N msgs'. P1 correct.

### SESS-07 [session-integrity] A panic never flushes the live conversation: the panic hook only restores the terminal, so mid-turn progress held in LIVE_HISTORY is lost even though the exit-flush machinery exists exactly to prevent this

- **Vị trí:** `src/ui/tui.rs:664` · verdict CONFIRMED
- **Hệ quả:** publish_live_history keeps a coherent in-memory transcript at every iteration boundary precisely so abrupt death doesn't lose the turn's work — but on panic (the most likely in-process crash) nothing writes it. The user restarts, gets a recovery offer that restores only the PRE-turn boundary (checkpoint at main.rs:3883-3887 runs before the user message is even pushed), and every assistant reply/tool result the turn had produced is gone despite having been safely snapshotted in memory.
- **Bằng chứng:** `std::panic::set_hook(Box::new(move |info| { emergency_restore(); prev(info); }));` (tui.rs:664-667) — no flush_live_session_on_exit. main.rs:6684 documents the snapshot as "Memory-only by design; the actual file write happens once, on exit", and the post-loop flush at main.rs:4181 is skipped when a panic unwinds out of the REPL loop.
- **Hướng sửa:** In install_panic_hook, call flush_live_session_on_exit() (it is synchronous file I/O, already designed to be safe from foreign threads) before emergency_restore/prev, guarded by catch_unwind like the console handler does; optionally also checkpoint_history from on_progress at a low cadence so kill -9/power loss keeps mid-turn tool results too.
- **Verifier:** Panic hook (src/ui/tui.rs:658-668) calls only emergency_restore() + prev(info) — no flush_live_session_on_exit; the only catch_unwind in the tree is the Windows console handler (main.rs:6707). The agent loop runs INLINE in the REPL task (tokio::select! at 4031-4038, not spawned), so a panic in it unwinds past the post-loop flush at 4181; Cargo.toml deliberately keeps panic=unwind (comment at lines 124-126). checkpoint_history has only 3 call sites (3883 pre-user-push WaitingModel, 4169 turn-end Idle, 6757 autosave) — nothing mid-turn — and publish_live_history is 'Memory-only by design' (6684). So the recovery offer after a panic restores only the pre-turn boundary + draft, losing all mid-turn assistant/tool output that was sitting in LIVE_HISTORY. P1 correct.

### SESS-08 [session-integrity] Every-turn autosave failures are swallowed with `let _` — a disk-full/locked sessions dir means nothing persists for the whole session and the user is never told

- **Vị trí:** `src/main.rs:6752` · verdict CONFIRMED
- **Hệ quả:** The startup hint and /resume promise "every turn was already autosaved" (comment at 3747-3749). If ~/.aizen/sessions is unwritable (full disk, AV/OneDrive lock, permission damage), the promise silently becomes false: the user works for hours believing the transcript is on disk, quits, and there is nothing to resume — with zero warnings during the session.
- **Bằng chứng:** autosave_last (6752-6757): `let _ = save_session(history, &name); if name != "last" { let _ = save_session(history, "last"); } … let _ = crate::core::recovery::checkpoint_history(…)` — all three writes discard errors. Contrast the turn-end recovery checkpoint at 4169-4175 which DOES surface failure: `tui::emit_line(… "recovery checkpoint not updated: {e}" …)`.
- **Hướng sửa:** Latch the first autosave failure and emit one visible warning ("⚠ autosave failing: <err> — this conversation is NOT being saved") with a re-warn on error change, mirroring the pattern already used for the recovery checkpoint at 4169; keep subsequent identical failures quiet to avoid spam.
- **Verifier:** autosave_last (6742-6762): all three persistence writes discard errors — 'let _ = save_session(history, &name);' (6752), 'let _ = save_session(history, "last");' (6754), 'let _ = crate::core::recovery::checkpoint_history(...)' (6757-6761). The contrast site is real: the turn-end recovery checkpoint at 4169-4175 emits 'recovery checkpoint not updated: {e}'. The startup hint's promise 'Every turn was already autosaved' is verbatim at 3747. No warning surface exists anywhere for a failing autosave. P1 correct.

### SESS-09 [session-integrity] The "[handoff context from the previous session]" seed is counted as a leading system lane, so the next /config or /model silently deletes it via refresh_prompt_lanes_in_place

- **Vị trí:** `src/main.rs:4953` · verdict CONFIRMED
- **Hệ quả:** A user runs /handoff (whose whole point is carrying the distilled context into the fresh thread), later opens /config to tweak a setting or /model to switch models, and the handoff context vanishes from the conversation without any indication — the model suddenly loses the decisions/paths/gotchas it was working from, which reads as inexplicable amnesia.
- **Bằng chứng:** `history.splice(0..lead, lanes);` (4953) where `lead = agent::compact::leading_system_count(history)` and lanes is always just stable+dynamic. leading_system_count (src/agent/compact.rs:143-150) counts every leading system message except those starting with COMPACT_MARKER_PREFIX = "[Earlier conversation auto-compacted" — the handoff message pushed at main.rs:6094-6096 starts with "[handoff context from the previous session]" and is therefore swallowed by the splice.
- **Hướng sửa:** Give the handoff seed the same protection the compaction marker gets: either start it with a recognized marker prefix that leading_system_count stops at, or have refresh_prompt_lanes_in_place preserve non-lane leading system messages (splice only the first 1-2 messages that match the lane fingerprint) — plus a unit test that /config after /handoff keeps the seed.
- **Verifier:** Chain fully verified: handoff seed starts with '[handoff context from the previous session]' (6094-6096, only occurrence in tree); leading_system_count (src/agent/compact.rs:143-151) counts every leading system message, exempting only COMPACT_MARKER_PREFIX = '[Earlier conversation auto-compacted' (compact.rs:25), so the seed is inside `lead`; refresh_prompt_lanes_in_place does history.splice(0..lead, lanes) at 4953 with lanes = stable+dynamic only (4945-4953), deleting the seed; it is called from /model (6320) and /config (6329). Per-turn refresh_dynamic_prompt_lane (4919-4932) touches only index 1, so the seed survives until the first /config or /model, then vanishes silently. P1 correct.

## P2 — 29 finding

### WS-8 [identity-scoping] No surface anywhere prints the resolved project root, slug, or memory/skill zone — every identity fork above is undetectable by the user

- **Vị trí:** `src/main.rs:5219` · verdict CONFIRMED
- **Hệ quả:** The owner's stated goal is maximal transparency, yet the single most consequential piece of hidden state — "which project does aizen think this is" — is never shown. The user had zones for System32 and HOME for days and could not have noticed: there is nothing to compare against. Every P0/P1 above stays invisible precisely because of this gap.
- **Bằng chứng:** tui::emit_line(&format!("{}  {model} · window {}{}", style("📊 context breakdown")...  — /context prints only token accounting; /init --status prints "{} file(s), {} chunk(s) — indexed {}" with no root/slug/path (main.rs:5781-5787); the startup banner prints a splash + keybind hint (main.rs:3729-3734). Grep over src/ shows no call site that ever displays project_root() or project_slug() to the user; the only indirect trace is the per-entry "[p:slug]" tag in /memory list (memory/mod.rs:605).
- **Hướng sửa:** Print one dim line at startup: "project <root> · zone <slug> · git ok|MISSING FROM PATH · index 2d old". Add the same block to /context (it already itemizes the system prompt). Cost: one line; benefit: every fork in WS-1/4/6 becomes immediately visible.
- **Verifier:** main.rs:5219 quote exact; /init --status (5781-5787) prints counts+age only; startup intro (3729-3734) is splash+keybinds; footer status_text (3613-3638) shows tokens/turns/approval/persona — no root/slug/cwd. Grepped every project_root()/project_slug() call site in src/: all are internal keying, zero display surfaces. Only indirect traces are per-entry tags: zone_tag '[p:slug]' (memory/mod.rs:605) and 'zone {z}' (mod.rs:709), which show an entry's zone but never the CURRENT resolved identity to compare against. P2 right.

### WS-9 [identity-scoping] Codebase index records its build root but never reads it: clones/worktrees sharing a remote silently share and clobber one index, and slug drift makes the model claim 'run /init' after the user already did

- **Vị trí:** `src/agent/codebase.rs:1400` · verdict CONFIRMED
- **Hệ quả:** Two worktrees/clones of one remote share a single <slug>.json: ensure_fresh() in whichever checkout ran last silently rebuilds the shared index against ITS tree (codebase.rs:347), so retrieval in the other checkout injects chunks from a different branch state with no attribution. Conversely, after a WS-1 slug flip, codebase_search tells the model "no codebase index yet — run /init to build it first" (codebase.rs:1552) even though the user built it yesterday — a false statement presented as fact.
- **Bằng chứng:** root: root.to_string_lossy().into_owned(),  — written into CodebaseIndex at build; load() (codebase.rs:225-232) checks only `idx.version != INDEX_VERSION` and no caller anywhere reads `.root` (grep: zero matches for `idx.root` outside the write and a test). index_path() = codebase_index_path(project_slug()) (codebase.rs:215), and the slug deliberately keys on remote.origin.url so "a re-clone / worktree / moved checkout keeps the SAME memory zone" (config.rs:142-144).
- **Hướng sửa:** On load(), compare idx.root (normalized) against the current canonical project_root(); on mismatch keep serving but tag retrieval/`/init --status` with "index built from <other root>" and prefer a per-root index filename (slug + short root-hash) so worktrees stop overwriting each other. Change the 1552 error to distinguish "never built" from "built under a different project identity".
- **Verifier:** codebase.rs:1400 quote exact; load() (225-232) checks only idx.version; grep for '.root' in codebase.rs returns zero reads and CodebaseIndex is not consumed elsewhere (main.rs:5778/5874 use files/chunks/analysis only). ensure_fresh (347) + source_tree_drifted (310-311) compare the slug-shared index against the CURRENT project_root() tree and rebuild in the background — clones/worktrees sharing a remote URL share one slug (config.rs:142-144 comment verbatim) and clobber each other. 1552 error string exact and cannot distinguish never-built from slug-flipped. P2 right.

### WS-10 [identity-scoping] NG_PROJECT_ROOT split-brain: memory/skills/index/recovery follow the override while Time Machine and the workspace lease follow cwd

- **Vị trí:** `src/features/timemachine.rs:444` · verdict CONFIRMED
- **Hệ quả:** Set NG_PROJECT_ROOT to repo A while sitting in repo B (the documented escape hatch): memory facts, learned skills, the codebase index and crash-recovery scope are all attributed to A, but every checkpoint, rewind and exclusive lock lands on B. `checkpoint_rewind` then honestly reports rewinding "the project" while the memory system believes the project is a different directory — two subsystems disagree about identity with no surface reporting either.
- **Bằng chứng:** fn current() -> Result<Self> { Self::discover(&std::env::current_dir().context("resolving cwd")?) }  — RepoContext never consults NG_PROJECT_ROOT, while project_root() honors it first (config.rs:59-63) and calls it "an escape hatch"; the writer lease likewise anchors on cwd: `std::env::current_dir().and_then(|p| p.canonicalize())` (agent/mod.rs:1912-1914).
- **Hướng sửa:** Route timemachine's RepoContext::current() and the lease acquisition through project_root() (falling back to cwd only when the override is unset), or explicitly refuse the combination: if NG_PROJECT_ROOT is set and differs from the cwd-derived repo root, print a startup warning naming both paths and which subsystem uses which.
- **Verifier:** timemachine.rs:443-445 quote exact (cited 444, the discover-on-cwd call); grep confirms NG_PROJECT_ROOT appears in 10 files but NOT features/timemachine.rs; the writer lease anchors on cwd at agent/mod.rs:1912-1916; project_root() honors the override first (config.rs:59-63, doc calls it 'an escape hatch'). Memory/skills/index/recovery all route through project_root()/project_slug() and follow the override while checkpoints+locks follow cwd. No warning surface for the disagreement. P2 right.

### WS-11 [identity-scoping] hostbot /cd is process-global: one chat's /cd silently retargets every other bot/chat's cwd, project slug, memory zone and file tools

- **Vị trí:** `src/hostbot/daemon.rs:420` · verdict CONFIRMED
- **Hệ quả:** In a multi-bot/multi-chat deployment, user X running /cd in one Telegram chat instantly moves user Y's file tools, memory zone, and checkpoint repo to X's directory — mid-conversation, with no message in Y's chat. The comment documents the /sh+agent coupling but not the cross-chat blast radius, and /pwd is the only way to notice after the fact.
- **Bằng chứng:** match std::env::set_current_dir(p) {  — under the comment "`/cd <path>` + `/pwd` — the daemon is one process, so set_current_dir affects BOTH /sh and the agent" (daemon.rs:406-407). The daemon hosts multiple hot-added bots (/addbot) with persistent per-chat sessions under ~/.aizen/hostbot/, and project_slug()'s cache key includes cwd (config.rs:149-153) so the zone flips for every consumer at the next call.
- **Hướng sửa:** Store cwd per chat session (the ExecutionContext plumbing from exec-ctx P1-A is the natural carrier) and have file/shell tools resolve paths against the session's cwd instead of the process cwd; short of that, broadcast a "cwd changed to … by chat <id>" notice to every active chat and document /cd as daemon-global.
- **Verifier:** daemon.rs:420 quote exact under the 406-407 comment (which documents the /sh+agent coupling but not the cross-chat/cross-bot blast radius). Single process hosts the primary bot plus /addbot extras (daemon.rs:606, 644); set_current_dir is the only cwd mechanism (grep: 1 call site in src); project_slug()'s cache key includes cwd (config.rs:149-153) so the memory zone flips for every chat at the next call; no per-chat cwd and no broadcast notice exists. P2 right (visibility gap; the same-chat behavior is documented).

### WS-12 [identity-scoping] Slug key ingredients are mutable but uncached and unmigrated: `git remote add origin` (which the agent itself runs) silently re-keys the whole project at next launch

- **Vị trí:** `src/core/config.rs:149` · verdict CONFIRMED
- **Hệ quả:** A standard first-publish flow — work a while, then have aizen run `git remote add origin <url>` and push — moves the project from the path-hash key to the URL-hash key at the NEXT launch: all memory facts, skills and the /init index saved before the push become invisible. The in-session cache masks the change until restart, making the fork look like random amnesia days later (exactly the aizen-be twin observed: d4a1274e at 07:07, 750ac895 at 07:22 on 26/07).
- **Bằng chứng:** let cache_key = format!("{}|{}", std::env::var("NG_PROJECT_ROOT").unwrap_or_default(), std::env::current_dir()...);  — the cache is keyed only on env+cwd, while the hashed stable_key comes from git_remote_origin() / canonicalize() (config.rs:162-167) which can change underneath it. The codebase's own test suite acknowledges the fork class: "canonicalize() of a missing dir fails and yields a DIFFERENT slug than once it exists, which silently splits the zone" (src/memory/mod.rs:1252-1253) — but production has no detection or migration for any of these transitions.
- **Hướng sửa:** Persist the chosen stable_key per canonical root in a tiny map (~/.aizen/cli-memory/zones.json: canonical_root -> slug) and reuse it on every launch, updating the map (not the slug) when a remote appears; alternatively detect at startup that a zone exists under the path-hash for this root while the URL-hash resolves differently, and auto-alias or prompt to merge. Also invalidate the in-process cache when the agent itself mutates git remotes.
- **Verifier:** config.rs:149-153 quote exact: cache keyed only on (NG_PROJECT_ROOT, cwd) while the hashed stable_key (162-167) depends on mutable git state; the test comment at memory/mod.rs:1252-1253 is verbatim and names the exact fork class. Grep confirms production has no zones.json, no slug alias map, no migration/merge path anywhere in src. The observed aizen-be twin fits: d4a1274e is hash-verified as the path key of the now-deleted \\?\...\aizen_be, with the URL-keyed 750ac895 appearing 15 minutes later the same morning. P2 right.

### SW-07 [silent-failures] 'Reset self-memory' reports 'N item(s) cleared' even when deletion failed and nothing was removed

- **Vị trí:** `src/persona/self_mem.rs:605` · verdict CONFIRMED
- **Hệ quả:** This is a user-initiated deletion of accumulated persona memory (potentially personal/relationship notes). The count is computed BEFORE the delete and returned regardless of whether remove_dir_all succeeded (locked file, permission error on Windows), so the user is told their data was wiped while it remains on disk and keeps being injected into future prompts — a privacy-flavored false confirmation.
- **Bằng chứng:** pub fn reset(persona_slug: &str) -> usize {
    let n = list(persona_slug).len();
    let _ = fs::remove_dir_all(self_dir(persona_slug));
    n
} (self_mem.rs:603-607). Caller prints unconditionally: "reset self-memory ({n} item(s) cleared)" (src/main.rs:3081-3086). Same class: persona removal swallows the memory wipe at src/persona/mod.rs:173 (let _ = std::fs::remove_dir_all(self_mem::self_dir(...))).
- **Hướng sửa:** Make reset return Result<usize>: count files, attempt remove_dir_all, verify the dir no longer exists (or count survivors), and Err with the io error otherwise; the menu handler prints the error instead of the cleared line. Apply the same to the persona::remove path in persona/mod.rs:173.
- **Verifier:** self_mem.rs:603-607 exact: count computed BEFORE delete, remove_dir_all result discarded, count returned regardless. Caller prints 'reset self-memory ({n} item(s) cleared)' unconditionally at main.rs:3081-3086, and a surviving dir keeps injecting into future prompts. Secondary reference holds with one naming nit: the swallow at persona/mod.rs:173 is inside persona::delete() (not a 'remove' fn) — same claim, real line. P2 appropriate for the rare-trigger false deletion confirmation.

### SW-08 [silent-failures] Crash-recovery lease setup fails silently — checkpoint_history then no-ops for the whole session with zero surface

- **Vị trí:** `src/core/recovery.rs:124` · verdict CONFIRMED
- **Hệ quả:** If begin() fails once at startup (lock contention from a zombie process, unwritable ~/.aizen), ACTIVE stays None and every per-turn `checkpoint_history` returns Ok(()) forever — the crash/recovery sidecar system is dead for the entire session and neither the REPL nor any status command reports it. The user only finds out after a crash, when there is no recovery offer for the session they lost. The comment marks it best-effort, but a session-long silent disable of a data-protection feature deserves one visible line.
- **Bằng chứng:** let lock = match crate::core::repo_lock::RepoTxnLock::acquire(&lock_path(&dir), Duration::from_secs(1)) {
        Ok(l) => l,
        Err(_) => return,
    }; (recovery.rs:122-125; also `let _ = fs::create_dir_all(&dir);` at 120 and `if write_manifest(...).is_err() { return; }` at 142-143). Downstream: `let Some(active) = guard.as_mut() else { return Ok(()) };` (recovery.rs:163) makes every later checkpoint_history a successful-looking no-op.
- **Hướng sửa:** Keep begin() non-fatal but record the failure reason in a static (e.g. RECOVERY_DISABLED: OnceLock<String>) and emit one dim startup line ('crash recovery unavailable: {e}'); optionally expose it in /doctor or /config status so the degraded state has a surface.
- **Verifier:** recovery.rs:120 (let _ = create_dir_all), 122-125 (lock Err => return), 142-144 (write_manifest err => return) all exact; ACTIVE stays None and checkpoint_history returns Ok(()) via the guard at 163 for the whole session. Refutation attempt failed: grep across src for any recovery-status surface (doctor/status/'recovery unavailable') found nothing — the disabled state has zero visibility. P2 right: a data-protection feature silently dead all session, discovered only when recovery is needed and absent.

### SW-09 [silent-failures] Hostbot per-chat history persistence is fire-and-forget — 'restart keeps this chat's context' silently broken

- **Vị trí:** `src/hostbot/daemon.rs:328` · verdict CONFIRMED
- **Hệ quả:** The comment states the product promise; the discard breaks it invisibly. If ~/.aizen/hostbot/sessions becomes unwritable, every Telegram/Discord turn completes normally, but after any daemon restart all remote conversations reset to zero context. Nothing is logged on the server and nothing is sent to the chat, so the degradation is undetectable until users complain the bot 'forgot everything'.
- **Bằng chứng:** // Persist the updated history so a restart keeps this chat's context.
        let _ = store::save_session(platform.name(), &route, &chat.to_string(), history); (daemon.rs:327-328); store::save_session does propagate errors (atomic_write_owner_only?, store.rs:166-167) but the only caller discards them.
- **Hướng sửa:** On Err, log once per route to stderr (the daemon already logs poll errors) and optionally notify the owner chat on the first failure ('warning: session persistence failing — context will not survive restart: {e}'), latching to avoid spamming every turn.
- **Verifier:** daemon.rs:327-328 exact including the promise comment; store::save_session returns Result (lock acquire + serde + atomic_write_owner_only, store.rs:151-168) and grep confirms daemon.rs:328 is its ONLY caller, discarding it. Nothing is logged server-side (the send path right below DOES eprintln on failure at 333-334, highlighting the asymmetry) and nothing reaches the chat. P2 right.

### SW-10 [silent-failures] Codebase index silently vanishes on INDEX_VERSION bump and background re-index failures are discarded

- **Vị trí:** `src/agent/codebase.rs:228` · verdict CONFIRMED
- **Hệ quả:** The user explicitly built the index with /init (a multi-second scan) and gets per-turn retrieval injection from it. After a binary upgrade that bumps INDEX_VERSION, load() returns None: retrieval and codebase_search silently stop, and /init --status claims 'no codebase index yet — run /init' with no hint that a versioned index exists and was discarded. Independently, if the debounced auto-rebuild in ensure_fresh keeps failing for a persistent reason (not just the benign lock case the comment covers), retrieval serves stale chunks forever with no surface anywhere.
- **Bằng chứng:** let idx: CodebaseIndex = serde_json::from_slice(&bytes).ok()?;
    if idx.version != INDEX_VERSION {
        return None;
    } (codebase.rs:227-230) — corrupt or version-mismatched index reads as 'no index at all'. Background freshness: `let _ = build_index(true, None, &|_| {});` (codebase.rs:386) inside ensure_fresh().
- **Hướng sửa:** In load(), distinguish the three states (absent / parse-failure / version-mismatch) and let /init --status print which one applies ('index built by older version — run /init to rebuild'); in ensure_fresh, store the last rebuild error in a static and expose it via /init --status instead of `let _ =`.
- **Verifier:** codebase.rs:225-231 exact: parse failure and version mismatch both collapse to None. /init --status (main.rs:5777-5796) calls the same load() and prints 'no codebase index yet — run /init to build it' (5794) for a version-discarded index. Finding actually UNDERSTATES: ensure_fresh computes drift via load_cached().map(...).unwrap_or(false) (codebase.rs:381), so when load() is None the auto-rebuild never fires either — a binary upgrade silently kills retrieval AND its self-healing. Background swallow let _ = build_index at 386 exact. P2 right.

### VIS-05 [visibility-surfaces] No surface anywhere shows project root or cwd — the identity that keys memory, skills, index, recovery and checkpoints

- **Vị trí:** `src/main.rs:3643` · verdict CONFIRMED
- **Hệ quả:** Since the REPL never calls set_current_dir, cwd-at-launch is the project identity forever — yet the user has no way to ask "which project does aizen think I'm in?". When launched from the wrong directory (system32-fbb7562a.md zone observed on disk from a shell launched in System32), everything — memory, skills, /init index, recovery scope — silently keys off the wrong identity with no visible symptom.
- **Bằng chứng:** format!("{model}{tok_chip}{turns_chip}{persona}{mode}{todos}{agents}") — the complete HUD field list (status_text, line 3613). The splash panel prints only "v{} · {} · {}" (version · model · endpoint) + key state (src/ui/splash.rs:420-423). Neither /tokens (print_status_line, 5508) nor /context (5186) prints root/cwd/slug either — grep of project_root()/project_slug() call sites shows zero UI renderers.
- **Hướng sửa:** Add one banner line to splash::render_inner — "project: C:\...\aizen · zone aizen-be-750ac895" — and a /where (or /status) command that prints: project root, cwd, zone slug, session file, git found yes/no, index age, LSP/MCP state. The banner line is one boxline() call; /where is a pure-print slash handler reusing existing getters.
- **Verifier:** main.rs:3643 is the complete HUD format string (model/tok/turns/persona/mode/todos/agents — no root/cwd/zone); splash render_inner (src/ui/splash.rs:405-423) prints only version·model·endpoint + key state; print_status_line (5508-5527) and print_context (5186-5209) show tokens/turns/telegram/ctx only. Grep of project_root() and project_slug() across src/ finds zero UI/display call sites; the only zone strings a user can ever see are per-entry tags and 'other workspaces' zones' in `aizen skill list --all-zones` (main.rs:8388-8393) — never the current identity. P2 stands.

### VIS-06 [visibility-surfaces] git-not-found is never surfaced: identity silently falls back to cwd and the time machine is silently dead until first use

- **Vị trí:** `src/core/config.rs:65` · verdict CONFIRMED
- **Hệ quả:** On this machine every Command::new("git") fails, so: repo-root detection is off (subdir launches create new identities), the remote-URL stable key is off (the observed zone-drift mechanism), and auto per-edit checkpoints/undo/redo are unavailable — all silently. The user believes the safety net (time machine, per-edit rewind) exists because /help and the tool docs advertise it.
- **Bằng chứng:** if let Ok(out) = std::process::Command::new("git").args(["rev-parse", "--show-toplevel"]).output() { ... } — falls through to std::env::current_dir() (line 73) on any failure, including 'git' not on PATH (the verified state of this user's PowerShell). No startup surface mentions git; the agent loop only emit_traces a checkpoint warning after an edit (src/agent/mod.rs:1556-1559), and /checkpoint surfaces git's raw error only when invoked.
- **Hướng sửa:** Probe `git --version` once at REPL start (it is one cheap shell-out); on failure print a banner/emit_line warning: "⚠ git not found on PATH — time machine & checkpoints disabled; project identity falls back to the launch directory". Also include the probe result in the proposed /where output.
- **Verifier:** config.rs:65 quote verbatim; fallback to std::env::current_dir() at line 73. No `git --version` probe or git-path resolution exists anywhere (grep for git_bin/git_exe/Program Files Git/resolve_git: no hits; every call is bare Command::new("git") — 8 sites in features/timemachine.rs alone). The only surfaces are per-edit emit_trace arms at agent/mod.rs:1551-1560 (the 'not a git repository' quiet arm and the 1556-1559 warning, which fire only AFTER an edit), exactly as the finding describes; startup says nothing about git, and the identity fallback is fully silent. P2 stands (checkpoint failure does trace after the first edit, so P1 would overstate; the silent-identity half is a visibility gap).

### VIS-07 [visibility-surfaces] /help has drifted from the slash catalog: 8 live commands and the '>' steer prefix are undocumented, despite the catalog module claiming drift is impossible

- **Vị trí:** `src/main.rs:5702` · verdict ADJUSTED
- **Hệ quả:** /handoff (context-preserving fresh thread), /resume, /recover (crash recovery), /diff and /undo/redo (time machine safety) are exactly the features a user needs in a bad moment — and the one discovery surface they will read (/help) does not mention them. The module doc's no-drift guarantee is itself false, so future commands will keep leaking out of /help.
- **Bằng chứng:** const SLASH_HELP: &str = "\ Commands: ..." — a hand-maintained const listing 28 commands; /handoff, /resume, /recover, /diff, /undo, /redo, /tools, /serve are dispatched by handle_slash but absent, and the 'Input shortcuts' section (5734-5738) lists #, !, @, !`cmd` but not the '>' steering prefix (src/ui/tui.rs:1619). src/features/slash.rs:3-4 claims "The picker, live input palette, and help text all consume this catalog so a command cannot be executable yet invisible", and its own test (line 142) is literally named init_and_currently_omitted_commands_are_catalogued.
- **Hướng sửa:** Generate SLASH_HELP from crate::features::slash::list() (name + argument_hint + description, skipping alias rows), making the doc claim true, and add the '>' prefix plus Alt+Enter steering to the Input shortcuts section. A unit test asserting every non-alias catalog name appears in the rendered help locks it in.
- **Verifier:** Core claim holds but the count is wrong: /undo and /redo ARE documented in SLASH_HELP — the /timeline row at main.rs:5719 reads '…/timeline pick to restore · also /undo · /redo'. The truly undocumented dispatched commands are SIX: /handoff (6082), /resume (5997), /recover (6025), /diff (6410), /tools (6365), /serve (6378). Verified: /help prints only the hand-maintained SLASH_HELP const (main.rs:5968), the Input-shortcuts section (5734-5738) omits the '>' steer prefix (implemented at src/ui/tui.rs:1618-1631), src/features/slash.rs:3-4 claims 'help text … consume this catalog' which is false (the catalog at slash.rs:32-102 does contain all six + undo/redo, so the picker/live palette DO show them — /help is the drifted surface), and the test at slash.rs:142 is named init_and_currently_omitted_commands_are_catalogued. Also minor: SLASH_HELP lists 29 commands, not 28. Severity P2 unchanged.

### VIS-08 [visibility-surfaces] /memory error path uses eprintln! while the retained TUI is up — the error is painted over and the command appears to do nothing

- **Vị trí:** `src/main.rs:6333` · verdict CONFIRMED
- **Hệ quả:** When /memory edit|forget|restore fails (bad id, ambiguous match, store I/O error), the user sees no output at all — indistinguishable from success or from a dead command. For a data-mutating command (forget/edit), silent failure means the user believes a fact was corrected or removed when it wasn't.
- **Bằng chứng:** "memory" | "mem" => { if let Err(e) = slash_memory(arg) { eprintln!("{} {e}", style("memory:").red()); } } — /memory is NOT in tui::slash_takes_stdin (src/ui/tui.rs:2444-2465), so it runs with the retained alt-screen renderer active, whose next frame overwrites raw stderr writes (the failure class documented in the repo's own '/memory println wiped' fix, which converted the cmd_* success paths to tui::emit_line but left this error path on eprintln!). Same pattern at 6367-6370 fallback ok, but /workflows'/agents' handlers are fine.
- **Hướng sửa:** Replace this eprintln! with tui::emit_line (mirroring the surrounding arms that already do so for lsp/approval/effort errors), and sweep handle_slash for the remaining eprintln! calls in non-suspending arms.
- **Verifier:** main.rs:6331-6334 verbatim: '"memory" | "mem" => { if let Err(e) = slash_memory(arg) { eprintln!(…) } }'. /memory is absent from tui::slash_takes_stdin (src/ui/tui.rs:2444-2465: config/setup/persona/personas/character/skills/skill/apps/integrations/telegram/tg/serve/sessions/model + timeline-pick/effort/tools-menu), so it runs with the retained renderer up. Refutation attempt failed: every neighboring eprintln arm (config 6325, persona 6338, skills 6343, apps 6348, telegram 6374, serve 6380) IS a suspending command from that list, so /memory is the one arm whose error genuinely lands on a wiped stderr; tui.rs:784-790 documents that eprintln is only correct when the TUI does not own the screen. P2 stands.

### VIS-09 [visibility-surfaces] On a turn error, the user's message is silently popped from history — the transcript shows a message the model context no longer contains

- **Vị trí:** `src/main.rs:4164` · verdict CONFIRMED
- **Hệ quả:** After an API error the screen still shows the user's question, but the next turn's context has no record of it: a follow-up like "as I said above…" silently refers to something the model never saw, and the display and the actual context disagree with no indication. The dropped text is also not in the autosave, so quitting after an error loses it entirely.
- **Bằng chứng:** Some(Err(e)) => { tui::emit_line(&format!("{} {e}", theme::err("error:"))); if history.last().map(|m| m.role == "user").unwrap_or(false) { history.pop(); } } — the popped message was already echoed to the transcript at line 3853 ("❯ {echo}"), and this arm (unlike the Esc/clarify arms at 4079/4089) never calls autosave_last, so the failed turn's text is also absent from the session file.
- **Hướng sửa:** Emit an explicit notice when popping — "(your last message was removed from context after the error — press ↑ to re-send)" — and pre-fill the input draft with the popped text via tui::set_draft (the mechanism /recover already uses at line 6046). Call autosave_last(&history) in this arm like the other non-success arms.
- **Verifier:** main.rs:4161-4166 verbatim: the Err arm emits the error then silently pops the trailing user message (pop at 4164); the message was echoed to the transcript at 3853-3857 ('❯ {echo}'). Unlike the Esc arm (autosave_last at 4079) and the clarify arm (4089), this arm never autosaves, so the popped text is absent from the named session file (only the recovery checkpoint at 4169 runs, with the already-popped history). No notice, no draft refill (tui::set_draft exists and is used by /recover at 6045). Display/context divergence + missing persistence confirmed. P2 stands.

### VIS-10 [visibility-surfaces] The live session's file name is never shown, and the /sessions picker is alphabetical with no dates and no 'current' marker

- **Vị trí:** `src/main.rs:6777` · verdict CONFIRMED
- **Hệ quả:** The user cannot answer "which file is THIS conversation being written to?" from any surface, and in the picker cannot tell four near-identical 'hello-*' sessions apart (no timestamps, no recency order, no current-session marker, no project). Restoring the wrong one silently replaces the live conversation with a stale twin.
- **Bằng chứng:** format!("{} {name}  —  {n} msg{}", ...) — the entire per-session row (sessions_menu); list_sessions() at 6619 does out.sort() (alphabetical, not recency). The slug is chosen silently in autosave_last (6746-6749: allocate_session_slug + set_session_slug with no emit), and /clear only prints "(new conversation)" (5980) without naming either the closed or the new file. Observed pool: 20+ files like hello-0723.json, hello-0723-2.json, hello-0725.json, hello-0726.json.
- **Hướng sửa:** Emit the allocated slug on first autosave ("· saving as “hello-0726”", one dim line); in sessions_menu sort rows by file mtime descending and append a relative age (fmt_time_ago already exists) plus a "● current" marker when name == current_session_slug().
- **Verifier:** main.rs:6777-6781: the entire picker row is icon + '{name}  —  {n} msg' — no date, no project, no current marker; list_sessions (6607-6620) ends with out.sort() = alphabetical; the slug is allocated silently in autosave_last (6746-6750, allocate_session_slug + set_session_slug, no emit); /clear prints only '(new conversation)' (5980) and set_session_slug(None) at 5975 without naming either file. status_text (3643) has no session chip. Observed pool includes hello-0723.json, hello-0723-2.json, hello-0725.json, hello-0726.json — indistinguishable in the picker. P2 stands.

### VIS-11 [visibility-surfaces] /init --status reports an index without saying which project/root/file it is for — slug drift makes the index 'vanish' with no explanation

- **Vị trí:** `src/main.rs:5782` · verdict CONFIRMED
- **Hệ quả:** When the slug drifts (or aizen is launched from a subdir/wrong dir), yesterday's /init lives under another key and /init --status says "no codebase index yet" — the user rebuilds a 41k-file index for nothing, or worse assumes codebase_search/auto-retrieval is working when it is silently reading nothing. Nothing printed lets them see the key mismatch.
- **Bằng chứng:** "{} {} file(s), {} chunk(s) — indexed {}" — the whole status output (plus analysis summary); the None arm prints only "no codebase index yet — run /init to build it" (5793-5795). The index path is keyed on the drifting slug: src/agent/codebase.rs:214-216 fn index_path() { config::codebase_index_path(&config::project_slug()) }, and a leaked wrong-root fixture already sits in C:/Users/admin/.aizen/cli-memory/codebase/ (ng-workdir-large-mono-24-90d53468.json with "root":"/x").
- **Hướng sửa:** Print identity with the status: root path, zone slug, and the index file path (index_path().display()) in both the Some and None arms — the None arm becomes "no index for zone aizen-be-d4a1274e (looked at <path>)", which makes a drift instantly diagnosable and pairs with the VIS-01 zone surfaces.
- **Verifier:** main.rs:5781-5787 (Some arm: '✓ codebase index: {n} file(s), {n} chunk(s) — indexed {ago}' + analysis summary) and 5793-5795 (None arm: 'no codebase index yet — run /init to build it') print no root/zone/path. index_path() at src/agent/codebase.rs:214-216 keys on config::project_slug(); codebase::load() (225-232) checks only the version field — it never compares idx.root to the current project_root, so a drifted slug yields the None arm with zero explanation and a stale index under another key is silently unread. The wrong-root fixture is real: ~/.aizen/cli-memory/codebase/ng-workdir-large-mono-24-90d53468.json (160 bytes) begins {"version":2,"root":"/x",…}. P2 stands.

### F4 [config-dead-state] 1.45 GB of orphaned model weights under ~/.aizen/models with no surface to see or clean them

- **Vị trí:** `src/core/config.rs:330` · verdict CONFIRMED
- **Hệ quả:** The user's shipped binary is a default (non-dense) build where models_dir() is explicitly inert, so 1.45 GB sits dead in the home dir. No `aizen` command lists ~/.aizen storage or models, so the user cannot know this exists, why, or that it is safe to delete.
- **Bằng chứng:** #[allow(dead_code)] // used by `--features dense` (model2vec loader); inert in the default build" on models_dir(). Observed: models/spike-p0-1 = 948 MB (contains HF-cache-style dirs jamie8johnson/, jinaai/, nomic-ai/…; ZERO references to 'spike-p0-1' anywhere in the repo) and models/potion-multilingual-128M = 507 MB (referenced only by the manual CI bench .github/workflows/dense-bench.yml; the runtime default at config.rs:345 is now "potion-base-8M").
- **Hướng sửa:** Add a storage inventory surface (e.g. `aizen doctor` or `/status disk`) that sizes each ~/.aizen subdir and flags models not matching embed_model_name() on the current build as removable; add `aizen models list|rm`. One-time: warn about spike-p0-1 (unreferenced) and superseded potion-multilingual-128M.
- **Verifier:** config.rs:330 quote exact ('#[allow(dead_code)] // used by --features dense ... inert in the default build'); :345 default is potion-base-8M. Measured: models/spike-p0-1 = 948 MB (subdirs jamie8johnson/jinaai/mrsladoje/nomic-ai/ryandono; zero references to 'spike-p0-1' anywhere in the repo) + models/potion-multilingual-128M = 506 MB (only reference: .github/workflows/dense-bench.yml). Only 'aizen reach doctor' and 'aizen time doctor' exist — no storage/models inventory surface. P2 correct.

### F5 [config-dead-state] 14 live config knobs are settable only by hand-editing cli-config.json — absent from wizard, /config menu, and `config set`

- **Vị trí:** `src/core/cli_config.rs:100` · verdict CONFIRMED
- **Hệ quả:** Documented features (self-review needs roles.oracle; prompt-cache; output-token cap; strict prompt tier; Jina/GitHub reach keys) are unreachable through every advertised surface — a user can only discover and set them by reading source and hand-editing JSON, and the /lsp edits toggle silently loses its state across sessions.
- **Bằng chứng:** pub self_review: Option<bool>," — read at src/main.rs:3928 (`enable_self_review: cli_config::load().self_review.unwrap_or(false)`), but ConfigCmd::Set (src/main.rs:546-633), the /config hub (main.rs:7450-7460) and the wizard expose no way to set it. Same for prompt_tier (:67), eager_tools (:72), max_parallel_subagents (:103), workflow_tool (:107), prompt_cache (:158), max_tokens (:162), skill_registry (:170), roles.summarizer/oracle/apply (:233-239), reach.jina_api_key/github_token (:386-388, wizard sets only tavily at main.rs:7599). Bonus: lsp_edit_diagnostics (:96) claims 'Toggle live with /lsp edits on|off', but that handler (main.rs:6156-6164) only flips in-memory LSP state and never saves the field — the toggle silently reverts on restart.
- **Hướng sửa:** Extend `aizen config set` with flags for each remaining CliConfig field (incl. --oracle-model/--oracle-base-url/--oracle-api-key-ref, --jina-key, --github-token, --prompt-tier, --max-tokens, --prompt-cache, --eager-tools, --self-review, --skill-registry, --max-parallel-subagents, --workflow-tool) and make '/lsp edits on|off' persist lsp_edit_diagnostics via cli_config::save.
- **Verifier:** All field declarations verified at the cited cli_config.rs lines (:67 :72 :96 :100 :103 :107 :158 :162 :170, roles :233-239, reach :386/:388) and all are live (read at main.rs:3928/4320, main.rs:3667, main.rs:5462, task_tool.rs:735, builtin.rs:298, client.rs:657/855, skills/registry.rs:104, agent/mod.rs:164/223). ConfigCmd::Set (main.rs:546-633) covers none of them (it does cover roles.subagent_default — the finding correctly excluded it); the /config hub's 7 sections (main.rs:7450-7484) don't either; websearch editor is tavily-only (main.rs:7599-7616). Bonus claim verified: '/lsp edits' (main.rs:6156-6165) only calls LSP.set_edit_feedback and never saves, while main.rs:5462 reloads lsp_edit_diagnostics from config at startup — the toggle silently reverts. One nuance (not verdict-changing): reach.jina/github also have env fallbacks (cli_config.rs:398/402), but those are undocumented, so no advertised surface exists.

### F6 [config-dead-state] ~20 behavior-changing env vars are undocumented, including NG_PROJECT_ROOT — the only escape hatch for the broken git-based project identity

- **Vị trí:** `src/core/config.rs:59` · verdict ADJUSTED
- **Hệ quả:** An env var set once in a shell profile (e.g. AIZEN_ULTIMATE, AIZEN_NO_EAGER, NG_SUMMARIZER_MODEL) silently overrides the visible config with no doc trail and no display of the effective source; conversely, users hitting the timeout/stall/project-root problems cannot discover the escape hatches that exist for exactly those problems.
- **Bằng chứng:** if let Ok(v) = std::env::var("NG_PROJECT_ROOT") {" — with git missing from PATH this is the only way to pin project identity, yet README.md/docs/FEATURES.md document neither it nor: NEXTGEN_HOME (config.rs:21), NG_NO_SCOPE(:81), NG_NO_GRAPH(:99), NG_GRAPH_EXPAND(:107), NG_EMBED_MODEL(:338), NG_MCP_REGISTRY (app_catalog.rs:23), NG_<ROLE>_MODEL/BASE_URL/API_KEY + NG_MODEL_<ID>_BASE_URL/API_KEY (cli_config.rs:280-288, 333-338), AIZEN_SHELL_TIMEOUT_SECS (builtin.rs:30, default 120s clamp 10..3600), AIZEN_STREAM_STALL_SECS (client.rs:291, default 90s clamp 15..1800), AIZEN_NO_EAGER (main.rs:3664), AIZEN_NO_STICKY (main.rs:3494 — swaps the whole UI to plain mode), AIZEN_NO_TIPS, AIZEN_ONBOARD, AIZEN_EMOJI (icons.rs:48), AIZEN_LOGO (splash.rs:266), AIZEN_AUTO_EFFORT/AIZEN_ULTIMATE/AIZEN_ADAPTIVE_EFFORT (cli_config.rs:526/535/544 — silently override the saved config), AIZEN_CODE_DENSE(+_COVERAGE) (codebase.rs:1434/1444), AIZEN_REACH_<CHANNEL>_BACKEND (reach/mod.rs:82), AIZEN_JINA_API_KEY/AIZEN_GITHUB_TOKEN/AIZEN_DISCORD_BOT_TOKEN (cli_config.rs:397-402/568). Documented set is only: AIZEN_INSTALL, MENU, NERD, NO_ICONS, TELEGRAM_TOKEN, BASE_URL, API_KEY, MODEL, HOME, BROWSER_CDP, ALLOW_PRIVATE_NET, MEM_DENSE, MEM_FUZZY, YES, DISCORD_WEBHOOK, SLACK_WEBHOOK, WEBHOOK_URL.
- **Hướng sửa:** Add an 'Environment variables' section to README/FEATURES enumerating every read var with default and effect; make `aizen config show` print any AIZEN_*/NG_* override currently active (name + which field it shadows, values masked for keys) so effective config is always explainable.
- **Verifier:** Core claim holds — every cited read-site verified real (NG_PROJECT_ROOT config.rs:59, NG_NO_SCOPE :81, NG_NO_GRAPH :99, NG_GRAPH_EXPAND :107, NG_EMBED_MODEL :338, NG_MCP_REGISTRY app_catalog.rs:23, AIZEN_SHELL_TIMEOUT_SECS builtin.rs:30, AIZEN_STREAM_STALL_SECS client.rs:291, AIZEN_NO_EAGER main.rs:3661, AIZEN_NO_STICKY main.rs:3475/4189, AIZEN_AUTO_EFFORT/ULTIMATE/ADAPTIVE_EFFORT cli_config.rs:526/535/544, AIZEN_CODE_DENSE codebase.rs:1434/1444, AIZEN_REACH_<CH>_BACKEND reach/mod.rs:81) and none appear in README.md or docs/FEATURES.md. Two corrections: (1) NEXTGEN_HOME IS documented — README.md:331 ('NEXTGEN_HOME is still honored') and CHANGELOG.md:224 — drop it from the undocumented list; (2) AIZEN_ULTIMATE and AIZEN_AUTO_EFFORT are not fully silent: /ultimate and the effort toggle print explicit override notes when the env is set (main.rs:6247-6248 and 4671-4672), though only at toggle time, never in 'config show'. Severity P2 unchanged.

### F7 [config-dead-state] cli-memory/core/active holds duplicate + orphaned per-repo memory cores with no surface to list, merge, or delete them

- **Vị trí:** `src/core/config.rs:281` · verdict ADJUSTED
- **Hệ quả:** When the slug hash flips (git absent, canonicalize variance), the memory written under the old slug silently stops loading — the user's 'always-on' repo memory just vanishes with no error, and nothing in /memory or the CLI shows which core file is active or that orphans exist holding stranded facts.
- **Bằng chứng:** pub fn core_active_path(slug: &str) -> PathBuf { ... join("core").join("active").join(format!("{}.md", safe_core_slug(slug))) — observed in C:/Users/admin/.aizen/cli-memory/core/active/: TWO slugs per project (aizen-be-750ac895.md AND aizen-be-d4a1274e.md; aizen-web-6e885184.md AND aizen-web-8279a5b7.md) plus accidental-cwd cores system32-fbb7562a.md, admin-e4df4c78.md, web-81760523.md, and a legacy cli-memory/core.active.md at the root.
- **Hướng sửa:** Add a core inventory to `/memory` / `aizen memory`: show the resolved slug + active core path for the current session, list all core/active/*.md with sizes and last-write, flag files whose slug can't be reproduced from any known project, and offer merge/retire. Print the active slug at startup (one dim line) so identity drift becomes observable immediately.
- **Verifier:** Mechanism and data confirmed: config.rs:281-286 quote exact; frozen_core.rs:32 loads per-slug; observed cli-memory/core/active/ holds exactly the claimed duplicates (aizen-be-750ac895.md + aizen-be-d4a1274e.md; aizen-web-6e885184.md + aizen-web-8279a5b7.md) plus system32-fbb7562a.md, admin-e4df4c78.md, web-81760523.md, aizen-621459ef.md, and legacy cli-memory/core.active.md; no surface lists them (memory::inventory at memory/mod.rs:619 covers store entries only, not core files). CORRECTION to impact: every one of these files, including the legacy one, is 0 bytes — so no facts are stranded today and 'memory written under the old slug silently stops loading / orphans holding stranded facts' is a latent risk proven by the drift, not an observed loss. Keep P2 for the real, verified problem: slug identity drift with zero visibility of which core is active.

### F8 [config-dead-state] Dead credential record ~/.aizen/auth.json (OAuth token schema) — no reader, no writer, never cleaned

- **Vị trí:** `C:/Users/admin/.aizen/auth.json:1` · verdict CONFIRMED
- **Hệ quả:** A file shaped like stored OAuth credentials sits in the product's home dir with nothing using, refreshing, or expiring it — the user cannot tell whether it is load-bearing (it is not) and would reasonably fear deleting it; stale credential-shaped material lingering forever is also poor hygiene for a security-conscious product.
- **Bằng chứng:** 268-byte JSON (2026-06-15) with keys: schemaVersion, provider, scopes, secretRef, refreshSecretRef, updatedAt, proxyUrl, expiresAt (key names only; values not inspected). grep across src/ for 'auth.json' finds zero references; the live token caches are ~/.aizen/mcp-tokens/ (mcp_oauth.rs:90) and cli-config.json. Sibling mcp/auth-cache.json (3 bytes, '{}') is equally dead.
- **Hướng sửa:** Have `aizen doctor`/startup recognize known-obsolete files by name+schema, state explicitly 'auth.json is from a pre-0.4 version and is no longer used', and offer deletion. Going forward, any credential store the code drops support for should get an explicit migration/cleanup path in the same release.
- **Verifier:** auth.json verified: 268 bytes, modified 2026-06-15, top-level keys exactly as listed (schemaVersion, provider, scopes, secretRef, refreshSecretRef, updatedAt, proxyUrl, expiresAt — values not read). Grep across src/ for 'auth.json' and 'auth-cache': zero matches. Live token cache path is defined at src/agent/mcp_oauth.rs:88-91 (~/.aizen/mcp-tokens/, per-server files); mcp/auth-cache.json (3 bytes, '{}') equally dead. P2 correct.

### EXT-7 [external-deps] Writer-lease mutual exclusion silently degrades to per-directory scope when git is absent

- **Vị trí:** `src/core/workspace_txn.rs:46` · verdict ADJUSTED
- **Hệ quả:** Two aizen instances working in different subdirectories of the same repo compute different repo_id/worktree_id → different lock files under ~/.aizen/locks → the workspace writer lease no longer excludes them, and concurrent edits/time-machine writes can interleave. The lock still 'succeeds', so nothing anywhere indicates protection shrank from repo-scope to directory-scope.
- **Bằng chứng:** let repo_key = canonical_common_git_dir.as_ref().or(canonical_git_dir.as_ref()).unwrap_or(&canonical_root); — all three git_path() probes (lines 27-29) return None on binary-not-found (line 310: `.output().ok()?`), so repo_id keys on the lease-acquisition cwd (agent/mod.rs:1912 acquires with `std::env::current_dir()`)
- **Hướng sửa:** When all three git probes fail but resolve_git() found no binary (as opposed to "not a repo"), fall back to walking up for a `.git` directory/file marker to recover the repo root without the binary; if genuinely non-repo, keep directory scope but have /status and the lease-owner record note `scope: directory (git unavailable)` so a second instance's conflict diagnostics tell the truth.
- **Verifier:** Code evidence exact: workspace_txn.rs:46-49 repo_key fallback to canonical_root; git_path (:309-317) returns None on .output().ok()? binary-not-found; lease acquired with std::env::current_dir() at agent/mod.rs:1912-1916 -> two instances in different subdirs get different repo_id/worktree_id lock paths. BUT the claimed consequence ('concurrent edits/time-machine writes can interleave') is unreachable today when git is missing: every lease-holding write is separately blocked — agent edits by EXT-1's fail-closed checkpoint gate (auto_checkpoint is hardcoded true in production, not user-configurable), and the timemachine ops that take the lease (timemachine.rs:1586, 1695, 1717) themselves require git. What remains is a real but consequence-free hidden state: lock scope silently shrinks from repo to launch-directory with no surface — P2 (state exists, nothing shows it), escalating back to P1 the moment EXT-1 is fixed to the benign allow-edits path, so the two fixes must land together.

### EXT-9 [external-deps] project_root() silently falls back to cwd on any git failure — .aizen project config, /init index root and zones follow the launch directory

- **Vị trí:** `src/core/config.rs:73` · verdict CONFIRMED
- **Hệ quả:** With git off PATH the documented repo-root-aware behavior ("launching from a SUBDIR still finds the repo's .nextgen/") silently reverts to cwd-relative: project-local skills/mcp.json/personas aren't found from subdirs, /init indexes the subdir, and junk zones (system32) accumulate — with no line ever saying "project root = <path> (cwd fallback; git unavailable)".
- **Bằng chứng:** std::env::current_dir().unwrap_or_else(|_| PathBuf::from(".")) — reached whenever `Command::new("git").args(["rev-parse", "--show-toplevel"])` (line 65) errors, including binary-not-found; observed consequence: C:/Users/admin/.aizen/cli-memory/core/active/system32-fbb7562a.md (verified present) — a memory zone minted for a system32 cwd
- **Hướng sửa:** After the resolver reports git missing, walk up from cwd looking for a `.git` marker (cheap, no binary) before surrendering to cwd; whatever wins, show the resolved project root and its provenance (git toplevel | .git-marker walk | cwd fallback) once at startup and in /status; refuse to mint memory zones for obviously-foreign roots like %WINDIR%.
- **Verifier:** config.rs:65-73 verified: any git failure (incl. binary-not-found) falls through to current_dir(); the doc comment at :54-57 explicitly promises 'launching ng from a SUBDIR still finds the repo's .nextgen/' — that promise silently reverts to cwd-relative with git off PATH. Downstream consumers confirmed: .claude/agents dir (agents/mod.rs:165), codebase index root (codebase.rs:311, 1206), project mcp.json (mcp.rs:186), recovery (recovery.rs:84), and project_slug (config.rs:161). Observed junk zone re-verified: C:/Users/admin/.aizen/cli-memory/core/active/system32-fbb7562a.md (24/07/2026). No provenance surface: project_root() has zero display call sites; status_text shows nothing about root/zone. P2 right.

### ERR-08 [error-quality] The only user-visible record of a failed tool is the first 60 characters of the error's first line — with no way to see the rest

- **Vị trí:** `src/agent/mod.rs:2491` · verdict CONFIRMED
- **Hệ quả:** Long, information-dense failures — "error: protected workspace change was not run because the pre-edit ch…", a file_edit near-miss diff hint, a shell stderr tail — are amputated at 60 chars in the transcript. The user watches the model retry against an error they were never allowed to read, which breaks the "maximally explicit" goal exactly when things go wrong.
- **Bằng chứng:** mod.rs:2490-2491 in `summarize_result`: `if let Some(reason) = trimmed.strip_prefix("error:") { return (false, format!("error: {}", first_line_clip(reason.trim(), 60))); }` — this digest is what `tool_call_end` renders (tui.rs:835-860); the full result string goes only to the model. No overlay/expand affordance exists for tool results (the text overlay in tui.rs:98-111 serves other surfaces).
- **Hướng sửa:** For `ok == false` results, keep the 60-char digest on the row but additionally emit the full first ~3 lines of the error as an indented dim block under the tool row (like emit_edit_diff does for diffs), or add a keybinding that opens the last failed tool result in the existing text overlay.
- **Verifier:** mod.rs:2488-2491 exact: error results are clipped to first_line_clip(reason, 60) and that digest is the ONLY user surface — emit_tool_result (2390-2416) renders just the digest via tool_call_end (tui.rs:835-861), and the full string goes only into the model's tool result. Refutation attempted: grepped retained.rs for any expand/click affordance on tool rows (none) and audited every text-overlay consumer — the overlay serves menus//workflows, never tool results. Failed edit_diff blocks are also suppressed for errors (2413 gates on ok). P2 right.

### ERR-09 [error-quality] Non-TTY / headless runs fabricate "the user declined this action" when no user was ever asked

- **Vị trí:** `src/agent/mod.rs:2154` · verdict CONFIRMED
- **Hệ quả:** In piped/CI/one-shot runs the model is told a human said no — so its final answer confidently reports "you declined the deletion, so I skipped it", which the user reads as a factual account of something that never happened. Nothing on stderr records that a destructive op was auto-blocked by policy, so the operator can't distinguish policy-deny from an actual failure.
- **Bằng chứng:** mod.rs:2149-2155: `if tool.is_destructive() && … && !approve(tool.name(), args) { return Some("error: the user declined this action".to_string()); }` — but `approve` at 3242/3253 silently auto-denies without any prompt: `if !std::io::stdin().is_terminal() { … return false; }`, and `ask_approval` (tui.rs:556-558) returns false whenever the TUI is inactive.
- **Hướng sửa:** Split the string: when `approve` denies due to non-interactivity, return "error: destructive action auto-denied (non-interactive session; re-run interactively or use --yes)" and emit one stderr line noting the auto-deny. Keep "the user declined" strictly for a real [n] answer.
- **Verifier:** Chain verified: mod.rs:2149-2155 mints 'error: the user declined this action' whenever approve() is false; approve() (3220-3253) safe-denies with no prompt and no stderr note when stdin is non-terminal (3242-3253, Telegram bridge excepted) and ask_approval returns false when the TUI is inactive (tui.rs:556-558). The doc comment at 3216-3219 confirms auto-deny is intended policy — but the string fed to the model still asserts a human action that never happened, and nothing on any stream records the policy deny. Interactive one-shot (stdin a TTY) does prompt at 3255-3265, so the finding's scope (piped/CI/headless) is exactly right. P2 right.

### ERR-10 [error-quality] send_with_retry backs off silently (up to 3 retries, Retry-After capped 30s each) and the transient-retry lines never name the actual error

- **Vị trí:** `src/llm/client.rs:354` · verdict CONFIRMED
- **Hệ quả:** A rate-limited user watches the "thinking" spinner freeze for up to ~90 seconds with zero explanation — indistinguishable from the historical stall bug they already distrust (memory: sse-stream-stall). And when a retry line IS shown, "API error" hides whether it's a quota problem they could fix versus provider flakiness they should wait out.
- **Bằng chứng:** client.rs:354-358: `if is_retryable_status(status.as_u16()) && attempt < MAX_RETRIES { let delay = retry_after_ms(&resp).unwrap_or_else(|| backoff_ms(attempt, BASE_MS, CAP_MS)); attempt += 1; tokio::time::sleep(…).await; continue; }` — no note to any surface (contrast `stream_retry_note` at 309-315 which does emit). The loop-level line is opaque too: mod.rs:1094-1096 `goal_retry_line(&format!("API error; retry {attempt}/{}", cfg.max_transient_retries), delay)` — never says 429 vs 500 vs timeout.
- **Hướng sửa:** Emit a faint line on each in-client retry: "⟳ HTTP 429 from upstream — retrying 2/3 in 8s (Retry-After honored)" via the same TUI-funnel pattern stream_retry_note uses. Pass a short error summary (status code or 'timeout') into goal_retry_line's reason instead of the constant "API error".
- **Verifier:** client.rs:354-358 and 363-369: both retry branches sleep silently (up to MAX_RETRIES=3, Retry-After capped at 30_000ms at line 331 → up to ~90s of unexplained spinner). Contrast pattern verified: stream_retry_note (309-316) proves the codebase already has a TUI-safe retry-note funnel used for the blank-stream case only. The loop-level line is confirmed opaque: mod.rs:1094-1096 hardcodes 'API error; retry {attempt}/{}' with no status/kind. No spinner status text is updated during in-client retries. P2 right.

### ERR-11 [error-quality] Post-turn memory learning swallows all errors — the 'remembered N facts' feature can die permanently with no surface

- **Vị trí:** `src/main.rs:4725` · verdict CONFIRMED
- **Hệ quả:** If the memory store becomes unwritable or a store file is corrupted, auto-learning silently stops. The user — trained by the "🧠 remembered N facts — /memory to view" lines (4730-4739) to believe aizen retains what they say — keeps stating preferences that vanish. Absence of a confirmation line is not a signal anyone reads as breakage; this is exactly the audited 'memory rất ngu' complaint class.
- **Bằng chứng:** main.rs:4723-4726: `let report = match learning::ingest(&user_text, &opts) { Ok(r) => r, Err(_) => return, // best-effort; never disrupt the REPL };`
- **Hướng sửa:** Keep it non-fatal but not silent: on Err, emit one dim line per session ("memory: auto-learn failed ({e:#}) — /memory doctor to inspect") guarded by a once-flag, mirroring the ERR-03 pattern.
- **Verifier:** main.rs:4723-4726 exact: `Err(_) => return, // best-effort; never disrupt the REPL` — the error value is not even bound. The habituation claim is anchored: the '🧠 remembered N facts — /memory to view' line at 4730-4739 and the session-note line at 4740-4750 only fire on Ok, so a permanently failing store is indistinguishable from 'nothing worth learning this turn'. No once-per-session warning, health-chip change, or log exists on the Err path. P2 right.

### SESS-10 [session-integrity] Restoring a session resets none of the per-conversation state: todos, cost meter, destructive-op session grants, and L2 session memory silently carry over from the previous thread

- **Vị trí:** `src/main.rs:6537` · verdict CONFIRMED
- **Hệ quả:** After restoring conversation B over conversation A, the HUD cost tally still includes A's spend, the todo list still shows A's tasks (and the model sees them via todo tooling), previously granted "allow for this session" destructive-op approvals from A remain armed for B's turns, and A's session working-memory notes are injected into B's dynamic lane. All of this is hidden state the user cannot see or attribute, in a flow that presents itself as switching conversations.
- **Bằng chứng:** load_session (6537-6546) touches only history/lanes/slug. Contrast the /clear arm (5971-5975): `crate::agent::todo::clear(); client::cost_meter().reset(); tui::reset_session_allow(); set_session_slug(None);` — none of these run on /resume, /sessions restore, or /recover accept (6038-6061).
- **Hướng sửa:** Factor the /clear resets into a switch_conversation() helper and call it from load_session and /recover accept (keeping the slug logic separate); at minimum reset session approval grants (safety-relevant) and print one line listing what was kept vs reset.
- **Verifier:** load_session (6537-6546) touches only history/lanes/slug; the sessions_menu restore path (6800-6810) adds only update_live_history + replay; /recover accept (6038-6061) likewise resets nothing. /clear (5970-5980) is the only arm doing todo::clear + cost_meter().reset + reset_session_allow (handoff does todo+allow but not cost, 6097-6098). The L2 session-memory claim holds: session_mem is injected into the dynamic lane by current_system_prompt_bundle (4878-4881) and cleared only by rebuild_system (4986 clear_process_session_mem) — load_session's refresh_dynamic_prompt_lane rebuilds the dynamic lane WITH the previous conversation's notes. P2 correct.

### SESS-11 [session-integrity] The /sessions picker and resume hints hide all provenance: no project, no timestamp, no model, alphabetical order, corrupt files shown as "0 msgs", counts inflated by hidden system lanes

- **Vị trí:** `src/main.rs:6777` · verdict CONFIRMED
- **Hệ quả:** Since the dir mixes every project's sessions (observed: aizen_be, aizen_web, cauut, omniroute files side by side), the user picking from name + msg-count alone cannot tell which project or day a session belongs to — making the foreign-restore mistakes of SESS-02/03 easy and invisible. A corrupt session shows as a plausible "0 msgs" entry instead of an explicit "(unreadable)", and message counts disagree with what the user perceives as messages.
- **Bằng chứng:** Picker rows are only `format!("{} {name}  —  {n} msg{}" …)` (6777-6781) over alphabetically sorted names (6619 `out.sort()`); session_len maps unparsable files to 0 (6623-6630 `.unwrap_or(0)`); /resume and the hint report raw vector length including the 2 system lanes (6546 `Ok(history.len())`, 6588 `last.len()`), while `conversation_len` (6593-6595) exists precisely to exclude them but is unused on these paths.
- **Hướng sửa:** Show per-row: relative mtime, msg count via conversation_len, and provenance (project dir/model once stored per SESS-02); sort by mtime descending; render parse failures as "(corrupt — cannot restore)"; use conversation_len in the startup hint and /resume confirmation.
- **Verifier:** All display claims reproduced: picker rows show only icon + name + raw msg count (6776-6781), names alphabetically sorted (6619 out.sort()), session_len returns 0 for unreadable/corrupt files (6623-6630 unwrap_or(0)) rendering them as '0 msgs', /resume prints raw history.len() (6546 → 6012 '{n} messages, context restored'), the hint uses last.len() (6588 → 3754). Even stronger than claimed: conversation_len (6593-6595) has ZERO callers repo-wide — it is fully dead code, not just unused on these paths. Cross-project mixing confirmed by the live dir listing. P2 correct.

## P3 — 9 finding

### SW-11 [silent-failures] Auto-learned skill save failure swallowed — feature quietly does nothing

- **Vị trí:** `src/main.rs:4545` · verdict CONFIRMED
- **Hệ quả:** The whole auto-skill pipeline (model call included — real tokens spent at main.rs:4510) ends in a write whose failure is invisible: if the skills zone dir is unwritable the feature degrades to a permanent silent no-op, indistinguishable from 'the model decided nothing was worth saving'. Deliberate best-effort with comment, so hygiene-level, but it is the terminal step of a user-visible promise (the 'learned skill' line).
- **Bằng chứng:** match skill::save_scoped(name, "", when, steps, true) {
        Ok(_) => tui::emit_line(... "learned skill '{name}'" ...),
        Err(_) => {} // best-effort
    } (main.rs:4539-4546)
- **Hướng sửa:** Replace `Err(_) => {}` with a dim one-liner ('could not save learned skill {name}: {e}') — the turn is already over, so one line cannot 'disrupt the REPL', and it converts a permanent invisible failure into a diagnosable one.
- **Verifier:** main.rs:4539-4546 exact: match on save_scoped with Err(_) => {} // best-effort, after a real summarizer-billed model call at 4510. Failure is indistinguishable from 'model declined to save'. Deliberate best-effort with comment at the terminal step of a user-visible feature — P3 hygiene rating is right.

### SW-12 [silent-failures] /sessions picker renders a corrupt session file as '0 msgs' — indistinguishable from an empty session

- **Vị trí:** `src/main.rs:6629` · verdict CONFIRMED
- **Hệ quả:** A truncated/corrupt saved conversation (e.g. one produced by a partially failed save) displays exactly like a legitimately empty one; the user learns the truth only after selecting it, when load_session errors with 'parsing session file'. The picker — the one surface for auditing saved sessions — actively misdescribes the file's state.
- **Bằng chứng:** std::fs::read_to_string(&path)
        .ok()
        .and_then(|s| serde_json::from_str::<Vec<Message>>(&s).ok())
        .map(|v| v.len())
        .unwrap_or(0) (session_len, main.rs:6623-6630), rendered as "{name}  —  {n} msg{s}" in sessions_menu (main.rs:6776-6781).
- **Hướng sửa:** Make session_len return an enum or Option distinguishing unreadable/corrupt from a real count, and render corrupt entries as '{name} — unreadable (corrupt?)' in the picker so the user can decide to delete or investigate instead of trusting a fake 0.
- **Verifier:** session_len at main.rs:6623-6630 exact (read .ok() → parse .ok() → unwrap_or(0)); rendered as '{name}  —  {n} msg{s}' in sessions_menu at 6776-6781. A corrupt file displays identically to an empty session; the truth surfaces only after selection when load_session errors ('parsing session file', 6540) via the eprintln at 6811. P3 right — the picker misdescribes but no data is lost by the display itself.

### VIS-12 [visibility-surfaces] Resume hint and /resume message counts include hidden system lanes; conversation_len, written to fix exactly this, is dead code

- **Vị trí:** `src/main.rs:6588` · verdict CONFIRMED
- **Hệ quả:** Every resume surface over-reports by the number of system/prompt-lane messages (2+), so a 3-exchange chat shows as "5 messages" — a small but systematic lie on the exact surfaces meant to build trust that nothing was lost, and the intended fix already exists unused.
- **Bằng chứng:** Some((named.unwrap_or_else(|| "last".to_string()), last.len())) — raw Vec<Message> length including the leading system prompt lanes; shown as "({n} messages)" at 3754 and "— {n} messages, context restored" at 6012 (load_session returns history.len()). Directly above at 6591-6595 sits fn conversation_len — doc: "for the resume hint, so it reports what the user recognizes as 'messages'" — whose only occurrence in the codebase is its definition.
- **Hướng sửa:** Call conversation_len at the three display sites: most_recent_session should return conversation_len(&last) (it already parsed the Vec), and load_session/resume should report conversation_len(history) instead of history.len().
- **Verifier:** main.rs:6588 verbatim: Some((named.unwrap_or_else(|| "last".to_string()), last.len())) — raw Vec length including leading system lanes (session files persist the full history; load_session runs migrate_legacy_prompt_lanes/refresh_dynamic_prompt_lane on those loaded lanes, proving they are in the file). Shown at 3754 '({n} messages)' and 6012 '— {n} messages, context restored' (n = load_session's history.len(), 6546). conversation_len (6593-6595), whose doc comment says it exists 'for the resume hint', has exactly one occurrence in the entire tree — its definition. Dead code + systematic over-count confirmed. P3 stands.

### F9 [config-dead-state] Unit-test artifact leaked into the real user home: cli-memory/codebase/ng-workdir-large-mono-24-90d53468.json

- **Vị trí:** `C:/Users/admin/.aizen/cli-memory/codebase/ng-workdir-large-mono-24-90d53468.json:1` · verdict CONFIRMED
- **Hệ quả:** Tests polluting the production home dir proves the NEXTGEN_HOME sandboxing convention is opt-in and has already been forgotten at least twice; each escape plants junk the user can't distinguish from real state, and a future escape could overwrite a REAL codebase index or memory file.
- **Bằng chứng:** {"version":2,"root":"/x","built_unix":0, "analysis":{"workspaces":["packages/a","packages/b","packages/c"]},"files":[]} — a synthetic fixture (root '/x', built_unix 0, fake packages) written to the REAL home via the codebase_index_path writer (src/core/config.rs:262). No creator in current tree (grep 'large-mono'/'ng-workdir' = 0 hits), i.e. a test ran without pinning NEXTGEN_HOME and its name has since changed. models/spike-p0-1 (F4) is the same failure mode at 948 MB.
- **Hướng sửa:** Make the sandbox structural instead of conventional: a test-only #[cfg(test)] guard in nextgen_home() that panics if the resolved home equals the real USERPROFILE path during `cargo test` (or a shared with_home() helper mandated by CI grep). Delete the leaked fixture as part of the doctor cleanup in F3/F10.
- **Verifier:** Fixture verified byte-for-byte: {"version":2,"root":"/x","built_unix":0, analysis.workspaces [packages/a,b,c], files:[]} — 160 bytes, written 2026-07-24 18:45, the ONLY file in cli-memory/codebase/. Grep across the whole repo for 'large-mono', 'large_mono', 'ng-workdir': zero hits, so no current test creates it (writer renamed/removed — a test escaped the NEXTGEN_HOME sandbox convention). Writer path confirmed at src/core/config.rs:262 (codebase index path builder). P3 correct.

### F10 [config-dead-state] Abandoned v0.1.0 scaffold: 6 dead files + 9 never-used dirs under ~/.aizen with no creator, reader, or writer in the current tree

- **Vị trí:** `C:/Users/admin/.aizen/manifest.json:3` · verdict ADJUSTED
- **Hệ quả:** Dead scaffolding doubles the apparent config surface: every one of these files/dirs is a place a user (or an LLM agent operating on the user's machine) may 'configure' something that will never take effect, and the empty hooks/plans/tasks dirs advertise features the product does not have.
- **Bằng chứng:** "platformVersion": "0.1.0", ... "components": { "skills": {"count": 0...}, "hooks": {...} }" — manifest.json, memory/MEMORY.md (which cites a spec 'docs/DOTNEXTGEN-GLOBAL-DESIGN.md' that no longer exists and points at '~/.nextgen/memory/' while real memory lives in cli-memory/), hooks/index.json, agents/index.json, commands/index.json, mcp/auth-cache.json, and empty dirs projects/ history/ file-history/ cache/ backups/ plans/ tasks/ logs/ scratchpad/ — all stamped 2026-05-08 15:10; grep across src/ finds no reference to any of them (agents/ and commands/ dirs themselves ARE read, but only for *.md cards — never their index.json). hooks/index.json advertises a hooks system that has no implementation anywhere in src/.
- **Hướng sửa:** Ship a one-time home-dir migration in the next release: remove (or move to ~/.aizen/attic/) the known v0.1.0 scaffold set after confirming each is semantically empty, and print one summary line of what was tidied. Keep an allowlist of live paths in code so `aizen doctor` can flag any unknown file that appears under ~/.aizen in the future.
- **Verifier:** Scaffold claims verified: manifest.json:3 '"platformVersion": "0.1.0"' exact; memory/MEMORY.md cites the nonexistent docs/DOTNEXTGEN-GLOBAL-DESIGN.md and ~/.nextgen/memory/; hooks/agents/commands index.json are all {schemaVersion:1, []}; grep across src/ finds zero readers (only recovery.rs:101 matches 'manifest.json' — its own per-lease file); agents/ and commands/ dirs are read only as card dirs (agents/mod.rs:161, features/commands.rs:56), hooks/ never; no hooks implementation exists in src/. Dirs re-checked: projects/ history/ file-history/ cache/ backups/ plans/ tasks/ logs/ all 0 entries. CORRECTION: scratchpad/ is NOT empty and NOT part of the untouched 2026-05-08 scaffold — it holds 8 files (PowerShell scripts + output, 2026-07-17), i.e. agent-written content with no code reference; drop it from the 'empty dirs' list (it is its own orphaned-content case). Severity P3 unchanged.

### EXT-11 [external-deps] schtasks /Delete idempotency depends on English stderr — breaks on localized Windows

- **Vị trí:** `src/features/cron.rs:256` · verdict CONFIRMED
- **Hệ quả:** On a non-English Windows locale (e.g. Vietnamese), schtasks prints a localized "cannot find" message, so removing an already-absent scheduled task — documented at line 253 as "not a hard error (idempotent remove)" — becomes a hard error, and `aizen cron remove` fails on a task that is already gone.
- **Bằng chứng:** if !err.to_lowercase().contains("cannot find") {
                bail!("schtasks /Delete failed: {}", err.trim());
- **Hướng sửa:** Branch on the exit code plus a pre-check (`schtasks /Query /TN <task>` exit status) instead of substring-matching localized stderr; treat "query says absent" as success for /Delete regardless of message text.
- **Verifier:** cron.rs:253-258 verified verbatim, including the ':253 Missing task is not a hard error (idempotent remove)' comment directly above the English-only substring match. schtasks localizes its error strings, so on a non-English Windows locale the documented-idempotent `aizen cron remove` of an already-absent task hard-fails ('schtasks /Delete failed: ...'). Windows-only branch (cfg!(windows), :248); unix path rewrites crontab without the issue. P3 right (narrow: localized OS + already-deleted task), fix direction (exit-code + /Query pre-check) sound.

### EXT-12 [external-deps] Repo map silently swaps churn ranking for mtime and labels it only 'by recency' — cause never disclosed

- **Vị trí:** `src/agent/repo_map.rs:119` · verdict CONFIRMED
- **Hệ quả:** The model (and a user reading the map) sees "top N source files by recency" with all churn tags absent and no hint that the intended hot-file ranking was unavailable because git could not run — the degradation is labeled but its cause and remedy are hidden, so nobody knows the map quality is reduced or why.
- **Bằng chứng:** let by = if churn.is_some() { "git churn" } else { "recency" }; — git_churn (line 170) returns None on binary-not-found via `.output().ok()?`, per its own doc "`None` when not a repo / git missing"
- **Hướng sửa:** When churn is None AND resolve_git() reports the binary missing, render the header as "by recency (git unavailable — churn ranking off)" so the degradation is attributed; the one-time resolver warning (EXT-3) covers the remedy.
- **Verifier:** repo_map.rs:119-120 verified verbatim: header flips to 'by recency' whenever churn is None; git_churn (:169-179) returns None on .output().ok()? binary-not-found and non-zero exit alike, per its own doc (:162-163 'None when not a repo / git missing'). All churn: tags vanish (:124 only when churn_n > 0) with no cause attribution anywhere in the rendered map, so the model/user cannot distinguish 'young repo, mtime is fine' from 'git broken, hot-file ranking silently lost'. P3 right — labeled degradation with hidden cause, no data loss.

### ERR-12 [error-quality] load_session labels every read failure "no saved session" — a permission/lock error reports the file as nonexistent

- **Vị trí:** `src/main.rs:6539` · verdict CONFIRMED
- **Hệ quả:** A session locked by a sync tool / AV scanner, or an ACL problem, tells the user their saved conversation doesn't exist — they conclude it was lost and may re-do work, when the file is sitting right there in ~/.aizen/sessions (observed populated on this machine).
- **Bằng chứng:** main.rs:6539: `let s = std::fs::read_to_string(&path).with_context(|| format!("no saved session '{name}'"))?;` — rendered at main.rs:6811 as `Err(e) => eprintln!("{} {e}", style("restore:").red())` (Display only, so the io cause is dropped and the asserted-but-possibly-false 'no saved session' is all the user sees).
- **Hướng sửa:** Only claim nonexistence when it's true: check `path.exists()` first (or match `e.kind() == NotFound`) and use two contexts — "no saved session '{name}'" for NotFound, "could not read session file {path}: {io error}" otherwise. Render restore errors with {e:#}.
- **Verifier:** main.rs:6539 exact: every read_to_string failure — including ERROR_SHARING_VIOLATION from AV/sync locks and ACL denials — gets the context 'no saved session {name}', and both render sites print Display only (6811 `eprintln!("{} {e}", style("restore:").red())` and the /resume path at 6005+), so the io cause is dropped and the false nonexistence claim is all the user sees. sessions dir observed populated on this machine (~/.aizen/sessions with shared last.json per the established facts), so the mislabel is reachable. P3 right for frequency, though the message is actively false when it fires.

### SESS-12 [session-integrity] autosave_session is a shell that ignores four parameters, and the call-site comment claims it 'names the session with a model call' — which it never does

- **Vị trí:** `src/main.rs:6597` · verdict CONFIRMED
- **Hệ quả:** Dead weight that actively misleads maintainers: the comment describes a model-naming behavior that does not exist, and the cancellable wrapper + fallback pretend there is a network call to protect against. Anyone auditing autosave behavior (exactly this data-safety-critical path) wastes time chasing a phantom model call.
- **Bằng chứng:** `async fn autosave_session(history: &[Message], _http: &reqwest::Client, _base_url: &str, _api_key: &str, _model: &str) { autosave_last(history); }` (6597-6605), while the call site says "`autosave_session` names the session with a model call, so it's cancellable too" (4154-4156) and wraps it in cancellable_slash with an autosave_last fallback (4157-4158) — both branches do the identical thing.
- **Hướng sửa:** Collapse autosave_session to autosave_last at both call sites (4157 and 4414), delete the wrapper and the stale comment, or — if model-based naming is still intended — implement it and keep the comment honest.
- **Verifier:** Verbatim: autosave_session (6597-6605) takes _http/_base_url/_api_key/_model and just calls autosave_last. The call-site comment at 4153-4156 claims it 'names the session with a model call, so it's cancellable too', and the cancellable_slash wrapper at 4157-4158 falls back to autosave_last — both branches identical. Second direct call at 4414 (plain REPL). No naming model call exists anywhere in the function. P3 correct.

## Lỗ hổng audit (critic) — chưa quét, cần vòng sau

- **Approval prompt: one ambiguous keystroke can silently arm an invisible, irrevocable session-wide yolo (approval-gate UX dimension never audited)** — While a destructive-op approval is pending, the TUI input thread steals bare y/n/a keys as the verdict but lets every OTHER key keep editing the draft — src/ui/tui.rs:1531-1543: `Key::Char('a') | Key::Char('A') => Some('a')` then `// y/n/a only — other keys still edit the draft / queue messages (Claude-style)`. So a user who starts typing a chat message at the prompt ('actually...', 'yes but wait', 'no, hold on') has its first letter consumed as the answer — and 'a' sets SESSION_ALLOW, pre-authorizing EVERY destructive op for the rest of the session. That grant is then invisible and unrevokable: src/ui/tui.rs:529-531 says it is 'in-memory + session-scoped' reset only by /clear; grep shows reset_session_allow() is called only at src/main.rs:5974 (/clear) and 6098 (/handoff); and the HUD chip logic at src/main.rs:3624-3632 shows '⚡ yolo' only for the persisted config mode (`} else if approval == ApprovalMode::Yolo {`) — session_allow_all() is consulted nowhere outside ask_approval. Net: mode display says 'ask' while behavior is yolo, with no surface, no revoke short of destroying the conversation, and it survives /resume of a different session. No finding in any of the 7 dimensions touches ask_approval/SESSION_ALLOW.

- **Token accounting shown to the user is never reconciled with the provider's real numbers — the context meter, auto-compact trigger, and $ estimate all mislead (token/cost dimension unaudited)** — The HUD context bar and auto-compaction both run purely on the chars/4 estimate: src/main.rs:5047-5048 `history.iter().map(agent::estimate_message_tokens).sum::<usize>() + agent::schema_overhead_tokens()`, fed to the meter at src/main.rs:3617 and to the compact trigger at src/main.rs:4449 `let pct = session_tokens(history) as f64 / window as f64 * 100.0;`. Meanwhile the agent loop itself DISTRUSTS that estimate and anchors on real usage — src/agent/mod.rs:1110-1119 (RealAnchor) and the comment at src/agent/mod.rs:849: '(common once the provider reports more tokens than chars/4 — code, Vietnamese)'. The real per-call prompt tokens are already available process-wide (client::cost_meter().last_call(), src/llm/client.rs:138) but the only UI consumer is the cache-hit label (src/main.rs:3674). For this Vietnamese-language owner the meter systematically understates fill, so '~60%' can be a full window and auto-compact fires after the provider starts 400ing. Rider: /cost's dollar estimate prices ALL prompt tokens at the full input rate (src/main.rs:5079 `let cost = p as f64 / 1_000_000.0 * pin ...`) directly next to its own 'cached @ ~0.1× in' note (src/main.rs:5095), overstating cost on cache-heavy sessions with no hint the estimate ignores caching.

- **Verify gate: 'verified' and 'never ran' are indistinguishable — silent no-op on toolchain spawn failure, and hard-disabled on every unattended path** — Two independent holes, neither in the findings list (which only covered the git-diff self-review at agent/mod.rs:2941). (1) src/agent/verify_gate.rs:202 `let child = command.spawn().ok()?; // spawn failure (no toolchain) → silent no-op.` (module doc line 7 admits 'timeout / project shape / spawn failure all degrade to a silent no-op'); the caller comment at src/agent/mod.rs:1158 confirms 'gate returns None and this turn falls through to Done'. Since a PASS prints a green line (agent/mod.rs:1175 verify_line) and a no-op prints nothing, the user's only signal that edits were never typechecked is the ABSENCE of a line — on this exact machine class (binaries off PATH, cf. git) that is a permanent silent downgrade. (2) The gate is hard-coded OFF on the paths that need it most: src/main.rs:1560 `enable_verify_gate: false,` for every Telegram serve turn, src/main.rs:1481 for run_agent_capture (cron/one-shot capture), and src/agent/workflow.rs:597 for workflow steps — while the interactive default is true (src/agent/mod.rs:511). Phone-triggered '/agent <task>' edits and scheduled jobs skip the quality bar the REPL advertises, and nothing discloses the difference.

- **Hostbot remote agent runs leave zero action trace anywhere: quiet:true suppresses every tool-call line and no surface can replay what was done** — src/main.rs:1557-1560 builds serve turns with `quiet: true` and run_tool_body only emits the tool trace when not quiet — src/agent/mod.rs:2175 `let seq = if !quiet { emit_tool_call(tool.name(), args) } else { 0 };` (the per-turn token trace is likewise gated at agent/mod.rs:1124). Combined with src/hostbot/daemon.rs:294 (`/agent <task>` runs at `ApprovalMode::Yolo`), a phone message can run shell commands and edit files on this machine while the phone shows only the final prose and the server console shows nothing. The full transcript with tool calls IS written to ~/.aizen/hostbot/sessions/ (one live telegram.default.*.json exists there, so this path is in real use — C:/Users/admin/.aizen/hostbot/sessions), but no command lists or renders it: SERVE_COMMANDS (daemon.rs:28-46) has /status and /resume (a message COUNT), nothing that shows actions taken, and the hostbot findings in the audit covered persistence/pairing but not this observability hole. For an owner who wants maximal transparency, the highest-autonomy surface is the least auditable one.

- **Cron subsystem entirely unaudited: scheduled jobs run yolo in an unpinned working directory (System32 on Windows) and their results are invisible** — src/features/cron.rs:167-174 runs the job via run_agent_capture with `crate::core::approval::ApprovalMode::Yolo` ('Unattended → yolo approval'). But nothing pins a working directory: the schtasks registration at cron.rs:268 (`let tr = format!("\\\"{exe}\\\" cron run {name}");`) sets no start-in directory, and Windows Task Scheduler defaults tasks to %WINDIR%\System32 — while run_agent_capture derives everything from `std::env::current_dir()` (src/main.rs:1470). So a job created inside a project runs its unattended yolo file edits and mints its project identity/memory zone from System32 — the exact fake-project pathology the audit observed as cli-memory/core/active/system32-fbb7562a.md, now with write access and no human watching. The job's pinned-model spec never records the project. Output goes only to an append-only ~/.aizen/cron/<name>.log (cron.rs:182-185) whose path is never printed after creation, and `cron list` (cron.rs:126-131) shows name/schedule/task with no last-run time, no success/failure, no log pointer. (~/.aizen/cron does not exist on this machine, so this is latent, not live — but it is a whole scheduled-autonomy subsystem the 7 dimensions never opened.)

- **MCP trust flow: one 'no' makes a project's shipped servers permanently invisible, and the trust key inherits the broken root identity** — The audit covered the trust-prompt write swallow but not the flow itself. (1) Dismissal is a black hole: src/agent/mcp.rs:235-236 returns None from project_trust_prompt() when the root is in `dismissed`, and the /mcp status builds its only project-server mention from that same function (mcp.rs:1698-1701 `let trust_note = match project_trust_prompt() {`). After declining once, no surface ever again says 'this repo ships N project MCP servers, currently dismissed — run aizen mcp trust'; the servers simply don't exist anywhere visible, though the stated design (mcp.rs:145 'they can `ng mcp trust` later') assumes the user remembers. (2) The trust key is `std::fs::canonicalize(project_root())` (mcp.rs:185-188) — the same project_root() the identity dimension proved falls back to cwd when git can't spawn. Nobody followed that fork into mcp_trust.json: a repo trusted in one launch context silently reverts to untrusted (project servers vanish from the toolset with no note) when launched from a subdirectory or when git resolution flips, and C:/Users/admin/.aizen/mcp_trust.json accumulates path-keyed entries that can never match again.

- **Audit modality gap: every finding is static-read; the retained-TUI output-wiping mechanism that 4+ P1/P2 findings depend on was never exercised at runtime** — Findings like 'MCP connect failures reported only via eprintln!, which the retained TUI wipes' (src/agent/mcp.rs:1543), '/memory error path uses eprintln! ... painted over', and the approval/HUD claims above all hinge on the runtime routing contract documented at src/ui/tui.rs:510-518 ('text emitted during a suspended menu must be sent to the render thread, NOT print!ed ... where resume's clear+redraw would wipe it') — behavior that only manifests with the render thread live. The audit ran zero runtime verification even though a local release build is known-good (memory: `cargo build --release` → working aizen 0.4.8 binary in ~4.5 min), so cheap high-value confirmations were skipped: launch from a non-repo dir and watch the misdiagnosed 'run git init' block fire; press 'a' mid-draft at an approval prompt; watch whether eprintln lines actually vanish under ratatui. At least the P0/P1 rows whose severity rests on 'the user never sees X on screen' should be tagged as runtime-unconfirmed or smoke-tested before the owner acts on the ranking.

- **Serve-side context trimming is silent: cap_session drops the oldest whole turns of a phone conversation with no notice to the chat** — src/main.rs:1510-1523 cap_session() hard-drops the oldest turns past SERVE_SESSION_MAX_MSGS (`history.drain(lead..i);`) and is called after every serve turn at src/main.rs:1589; mid-loop compaction usually summarizes first (main.rs:1572-1574 calls it 'a hard backstop'), but when the backstop does fire — long chats, or when the summarizer endpoint errors — earlier conversation is deleted, not summarized, and the Telegram user is never told. The only related surface is the /resume command whose entire purpose is a count ('how much context is kept', daemon.rs:31), which cannot distinguish 'compacted into a summary' from 'silently amputated'. The interactive REPL got a queryable compaction boundary marker precisely for this ('what makes compaction feel non-lossy', main.rs:5150, src/agent/compact.rs:19-23); the serve path has no equivalent, so the bot just starts forgetting with no ⊟ marker, no note, nothing — a promised-context behavior ('follow-ups keep context', main.rs:1525-1527) that degrades silently.

